# 双指针：对撞指针与快慢指针

> 对应 youngyangyang04/leetcode-master 双指针章节与 Sedgewick《Algorithms》双指针技巧。

## 一、背景与挑战
在有序数组或链表上，用两个指针协同移动把 $O(n^2)$ 降为 $O(n)$。典型：两数之和、环形链表、移除元素。

## 二、核心原理
- 对撞指针：左右向中间夹（数组有序时）。
- 快慢指针：一快一慢，用于链表环、中点。
利用有序性或相对速度减少扫描。

## 三、形式化与数学基础
对撞：若 `a[i]+a[j] < target` 则 $i$ 增（因 $j$ 减小只更小）；故每步必缩范围。快慢：相遇周期与环长相关。

## 四、代码实现
```python
def two_sum_sorted(a, target):
    i, j = 0, len(a) - 1
    while i < j:
        s = a[i] + a[j]
        if s == target:
            return i, j
        if s < target:
            i += 1
        else:
            j -= 1
    return None

def has_cycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow is fast:
            return True
    return False
```

## 五、与其他技术对比
哈希表两数之和 $O(n)$ 但需空间；有序对撞零空间。快慢指针是链表环判定唯一 $O(1)$ 空间法。

## 六、常见误区
- 无序数组用对撞指针（结果错误）。
- 快慢指针判环用 `==` 比较值而非节点身份 `is`。

## 七、与开源书/权威来源对应
- leetcode-master 双指针（对撞/快慢）。
- Sedgewick 讨论 two-pointer 范式。

## 八、面试题
1. 有序数组两数之和为何对撞正确？
2. 快慢指针如何找环入口？

## 九、演进与趋势
双指针与滑动窗口常被统一视为"维护两个边界"的泛化技术。

## 十、小结
对撞利用有序性，快慢利用速度差；二者都是 $O(n)$ 双指针范式。
