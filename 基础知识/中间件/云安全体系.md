# 云安全体系（密钥 / WAF / DDoS / 合规）

> 云安全是「责任共担模型」：云厂商保底层（物理/网络/ Hypervisor），用户保上层（应用/数据/配置）。云厂商提供托管安全服务：密钥管理、WAF、DDoS 防护、证书管理、合规审计。本篇按「解决的问题 → 原理 → 特性 → 选型关注点」拆解。

---

## 一、云安全责任共担模型

| 层 | IaaS 用户责任 | PaaS 用户责任 | SaaS 用户责任 |
|----|---------------|---------------|---------------|
| 数据/身份 | 全责 | 全责 | 全责 |
| 应用/运行时 | 用户 | 用户 | 厂商 |
| 操作系统 | 用户 | 厂商 | 厂商 |
| 网络/虚拟化 | 用户 | 厂商 | 厂商 |
| 物理/设施 | 厂商 | 厂商 | 厂商 |

> 核心认知：**用 PaaS/SaaS 越多，安全责任越往上层收**——但数据与身份（IAM）永远是自己的责任。

---

## 二、密钥管理服务（KMS）

### 2.1 解决的问题

应用配置里硬编码 AK/SK/数据库密码 → 泄露即灾难。需要：集中托管密钥、自动轮换、访问审计、与云服务原生集成。

### 2.2 原理

- **信封加密（Envelope Encryption）**：用 KMS 主密钥（CMK）加密数据密钥（DEK），DEK 加密实际数据。CMK 不出 HSM，DEK 随数据存储。
- **访问控制**：谁能使用 CMK 由 IAM 策略控制（与 IAM 原生联动）。
- **自动轮换**：CMK 每年/自动轮换，旧密钥仍可解密历史数据。

| 服务 | 厂商 | 关键点 |
|------|------|--------|
| AWS KMS | AWS | CMK + HSM（FIPS 140-2）、自动轮换、别名 |
| Azure Key Vault | Azure | 密钥/证书/机密三合一、HSM、软删除/清除保护 |
| GCP Cloud KMS | GCP | 与 IAM 集成、自动轮换、信封加密 |
| 阿里云 KMS | 阿里云 | 国密 SM2/SM4、信封加密、托管密码 |
| 腾讯云 KMS | 腾讯云 | 国密支持、HSM、密钥轮换 |
| HashiCorp Vault | 多云 | 动态密钥、多云统一、与 K8s 深度集成 |

**选型关注点**：国密合规（SM2/SM4）→ 国内云 KMS；多云统一密钥 → Vault；与云服务集成（S3/RDS 加密）→ 原生 KMS。

---

## 三、Web 应用防火墙（WAF）

### 3.1 解决的问题

应用层攻击（SQL 注入、XSS、CSRF、CC 攻击）——在流量进应用前拦截。

### 3.2 原理

- **反向代理模式**：DNS 指向 WAF → WAF 检查请求 → 干净流量转发到源站
- **规则引擎**：OWASP Core Rule Set（CRS）+ 自定义规则（IP/路径/速率/正则）
- **Bot 管理**：人机验证（CAPTCHA）、指纹、行为分析

| 服务 | 厂商 | 关键点 |
|------|------|--------|
| AWS WAF | AWS | 托管规则组（AWS/ Marketplace）、Bot Control、与 CloudFront/ALB 集成 |
| Azure WAF | Azure | OWASP CRS + 自定义规则、与 Front Door/Application Gateway 集成 |
| GCP Cloud Armor | AWS | 基于边缘的安全策略、自适应保护（ML）、与 Global LB 集成 |
| 阿里云 WAF | 阿里云 | 国密支持、防爬、 Bot 管理、合规等保 |
| 腾讯云 WAF | 腾讯云 | AI + 语义分析、Bot 管理、域名接入 |

**选型关注点**：CDN/边缘接入 → 与 CDN 联发的 WAF（CloudFront WAF/Cloud Armor）；应用层精细防护 → ALB/API Gateway 上的 WAF；等保合规 → 国内云 WAF。

---

## 四、DDoS 防护

### 4.1 的问题

大流量攻击（网络层 L3/4、应用层 L7）打垮服务——需要 Tbps 级清洗能力。

### 4.2 原理

- **Anycast 流量牵引**：攻击流量被 BGP Anycast 路由到全球清洗中心
- **流量清洗**：基于特征/行为/速率过滤恶意流量，干净流量回注
- **应用层防护**：CC 攻击通过 WAF + 速率限制 + 人机验证缓解

| 服务 | 厂商 | 关键点 |
|------|------|--------|
| AWS Shield | AWS | Standard（免费，基础防护）+ Advanced（L3/4/7，24/7 DRT 支持） |
| Azure DDoS Protection | Azure | Basic（免费）+ Standard（与 VNet 集成） |
| GCP Cloud Armor | GCP | Always-on L7 防护 + 边缘清洗 |
| 阿里云 DDoS 高防 | 阿里云 | Tbps 级清洗、国内节点、游戏/金融常用 |
| 腾讯云大禹 BGP | 腾讯云 | 多线路清洗、Anycast |

**选型关注点**：云上基础防护（Shield Basic）一般免费；大流量攻击 → Advanced/高防套餐（贵但必要）；混合云/多入口 → 统一高防 IP。

---

## 五、证书管理服务（SSL/TLS）

| 服务 | 厂商 | 关键点 |
|------|------|--------|
| AWS ACM | AWS | 免费公共证书、自动轮换、与 ALB/CloudFront/API Gateway 集成 |
| Azure App Service Certificates | Azure | 托管证书、自动续期 |
| GCP Certificate Manager | GCP | 托管证书、与 Global LB 集成 |
| 阿里云 SSL 证书 | 阿里云 | 免费 DV 证书、OV/EV 付费、与 CDN/SLB 集成 |

**解决的问题**：证书手动申请/续期麻烦且易过期 → 托管自动续期 + 与云服务原生集成。

**选型关注点**：云上 HTTPS 终止 → 原生 ACM（免费且自动轮换）；国密合规 → 国内云国密证书。

---

## 六、安全合规与审计

### 6.1 解决的问题

等保 2.0/ISO 27001/SOC 2/GDPR 合规要求：配置审计、漏洞扫描、合规报告。

### 6.2 服务

| 服务 | 厂商 | 关键点 |
|------|------|--------|
| AWS Security Hub | AWS | 聚合 GuardDuty/Inspector/Macie 发现、合规评分 |
| AWS Config | AWS | 资源配置变更记录 + 合规规则评估 |
| Azure Policy + Defender | Azure | 资源配置合规 + 安全态势管理 |
| GCP Security Command Center | GCP | 风险/合规统一视图 |
| 阿里云配置审计/云安全中心 | 阿里云 | 等保合规检查、漏洞扫描、基线核查 |
| 腾讯云主机安全/云审计 | 腾讯云 | 主机安全 + 操作审计 |

### 6.3 审计日志

- **AWS CloudTrail**：记录所有 API 调用（谁、何时、什么操作）
- **Azure Activity Log + Diagnostic Logs**：活动日志 + 资源诊断日志
- **GCP Cloud Audit Logs**：管理员/数据访问/系统事件审计
- **阿里云操作审计（ActionTrail）/ 腾讯云操作审计（CloudAudit）**

**选型关注点**：等保 2.0 → 国内云合规套件（等保合规检查/云安全中心）；跨国合规（GDPR/SOC2）→ AWS/Azure/GCP 合规报告；操作审计必须开启（所有云厂商）。

---

## 七、云原生安全（工作负载/容器/供应链）

| 层面 | 服务 | 说明 |
|------|------|------|
| 镜像扫描 | ECR/ACR/GCR 内置扫描 | 镜像推送时自动 CVE 扫描 |
| 运行时安全 | GuardDuty/Defender/云安全中心 | 异常行为检测（挖矿/提权） |
| 密钥注入 | Secrets Store CSI Driver / External Secrets | K8s Pod 自动注入 Vault/KMS 密钥 |
| 供应链安全 | SLSA/Sigstore/Cosign | 镜像签名 + 来源证明 |
| 网络安全组 | Security Group / 安全组 | 实例级虚拟防火墙（有状态） |
| 网络 ACL | NACL /  VPC ACL | 子网级无状态访问控制 |

**选型关注点**：容器化部署 → 镜像扫描 + 运行时安全 + 密钥注入 CSI；供应链安全 → Cosign 镜像签名；微分段 → Security Group + 网络策略。

---

## 八、选型速查

| 需求 | 首选 | 备选 |
|------|------|------|
| 密钥托管 | 云厂商 KMS | HashiVault Vault（多云） |
| 应用层防护 | 云厂商 WAF | Cloudflare WAF（多云） |
| DDoS 防护 | 云厂商原生（Shield/高防） | Cloudflare/Akamai |
| 证书管理 | 云厂商 ACM | Let's Encrypt + Cert-Manager |
| 合规审计 | 云厂商安全中心/Security Hub | 第三方合规 SaaS |
| 操作审计 | CloudTrail/ActionTrail/CloudAudit | — |
| 镜像安全 | 云仓库内置扫描 + Cosign | Aqua/Trivy |
| 国密合规 | 国内云 KMS/WAF/证书 | — |

---

## 九、AWS GuardDuty 深入

### 9.1 威胁检测能力

| 威胁类型 | 说明 | 检测方式 |
|----------|------|----------|
| 未授权访问 | 异常 API 调用 | 行为分析 |
| 挖矿检测 | EC2 上的挖矿行为 | 网络流量+进程 |
| 恶意软件 | 已知恶意 IP/域名 | 威胁情报 |
| 数据泄露 | S3 异常访问模式 | 访问模式分析 |
| 容器威胁 | EKS 异常行为 | 日志分析 |

### 9.2 GuardDuty 配置

```json
{
  "DetectorConfig": {
    "FindingPublishingFrequency": "FIFTEEN_MINUTES",
    "DataSources": {
      "CloudTrail": {
        "EventSources": ["s3.amazonaws.com", "eks.amazonaws.com"]
      },
      "VPCFlowLogs": {
        "Status": "ENABLED"
      },
      "DNSLogs": {
        "Status": "ENABLED"
      }
    }
  }
}
```

### 9.3 成本

| 数据源 | 价格 |
|--------|------|
| CloudTrail 事件 | $1.00/百万事件 |
| VPC Flow Logs | $0.50/GB |
| DNS Logs | $0.50/GB |
| EKS Audit Logs | $1.00/百万事件 |

---

## 十、Azure Defender 深入

### 10.1 Defender 覆盖范围

| 覆盖范围 | 说明 |
|----------|------|
| 服务器 | VM 漏洞/恶意软件检测 |
| 容器 | AKS 安全态势 |
| 存储 | Blob/文件共享异常访问 |
| 数据库 | SQL/MySQL 漏洞检测 |
| 网络 | DDoS/WAF 建议 |
| 应用服务 | Web 应用漏洞 |
| Key Vault | 密钥泄露检测 |

### 10.2 Defender for Cloud 定价层

| 层 | 说明 | 价格 |
|----|------|------|
| Free | 基础安全态势 | 免费 |
| Standard | 高级威胁检测 | 按资源计费 |
| Defender for Servers | P1/P2 | $15/$40/月 |
| Defender for Containers | 容器安全 | $0.001/秒/节点 |

---

## 十一、Google Chronicle 深入

### 11.1 Chronicle 架构

```
数据源 → Chronicle SIEM → 检测规则 → 告警
  │
  ├── 网络流量（VPC Flow）
  ├── 身份日志（Cloud Audit）
  ├── 终点日志（Agent）
  └── 第三方日志（Syslog）

核心能力：
  高速搜索：PB 级数据秒级搜索
  自动化：SOAR 集成
  威胁情报：内置 Google 威胁情报
```

### 11.2 Chronicle vs 其他 SIEM

| 维度 | Chronicle | Splunk | ELK |
|------|-----------|--------|-----|
| 搜索速度 | 秒级（PB 级） | 中 | 慢（大数据量） |
| 部署 | SaaS | SaaS/自建 | 自建 |
| 成本 | 按数据量 | 按数据量 | 免费（自建） |
| 适用 | 大规模安全分析 | 企业级 SIEM | 中小规模 |

---

## 十二、云数据分类分级

### 12.1 分类分级框架

| 级别 | 数据类型 | 安全要求 |
|------|----------|----------|
| L1 公开 | 产品文档/新闻 | 无特殊要求 |
| L2 内部 | 内部邮件/文档 | 访问控制 |
| L3 敏感 | 客户信息/财务 | 加密+审计 |
| L4 机密 | 密钥/证书/核心算法 | KMS 加密+严格审计 |
| L5 绝密 | 监管数据/合规数据 | 硬件安全模块 |

### 12.2 分类工具

| 工具 | 厂商 | 说明 |
|------|------|------|
| Macie | AWS | S3 数据自动分类 |
| Purview | Azure | 数据治理+分类 |
| DLP | GCP | 数据丢失防护 |
| 数据安全中心 | 阿里云 | 数据分类分级 |

---

## 十三、云合规（SOC2/HIPAA/PCI-DSS）

### 13.1 合规框架对比

| 框架 | 适用 | 核心要求 |
|------|------|----------|
| SOC 2 | SaaS/云服务 | 安全/可用/处理完整性 |
| HIPAA | 医疗数据 | 隐私保护+安全控制 |
| PCI-DSS | 支付卡数据 | 加密+访问控制+审计 |
| GDPR | 欧盟个人数据 | 数据主体权利+隐私设计 |
| 等保 2.0 | 中国 | 安全等级保护 |

### 13.2 云合规检查清单

| 检查项 | SOC2 | HIPAA | PCI-DSS |
|--------|------|-------|---------|
| 访问控制 | ✅ | ✅ | ✅ |
| 加密传输 | ✅ | ✅ | ✅ |
| 加密存储 | ✅ | ✅ | ✅ |
| 审计日志 | ✅ | ✅ | ✅ |
| 漏洞扫描 | ✅ | ✅ | ✅ |
| 渗透测试 | ✅ | ✅ | ✅ |
| 数据备份 | ✅ | ✅ | ✅ |
| 事件响应 | ✅ | ✅ | ✅ |

---

## 十四、云密钥管理深入

### 14.1 密钥管理最佳实践

| 实践 | 说明 |
|------|------|
| 最小权限 | 仅授予必要密钥访问权限 |
| 自动轮换 | 定期轮换（90 天） |
| 信封加密 | 主密钥加密数据密钥 |
| HSM 保护 | 敏感密钥用 HSM |
| 审计 | 记录所有密钥操作 |
| 多区域 | 跨区域密钥复制 |

### 14.2 HashiCorp Vault 集成

```bash
# Vault 动态密钥
vault mount -path=database database
vault write database/config/mysql \
  plugin_name=mysql-database-plugin \
  connection_url="{{username}}:{{password}}@tcp(mysql:3306)/" \
  allowed_roles="readonly" \
  username="vault" \
  password="vault"

# 生成动态凭证
vault read database/creds/readonly
# 返回临时用户名/密码（自动过期）
```

---

## 十五、云网络安全组深入

### 15.1 安全组 vs NACL

| 维度 | Security Group | NACL |
|------|---------------|------|
| 层级 | 实例级 | 子网级 |
| 状态 | 有状态 | 无状态 |
| 规则 | 只允许 ALLOW | ALLOW + DENY |
| 优先级 | 所有规则同时生效 | 按规则号顺序 |
| 适用 | 实例级精细控制 | 子网级批量控制 |

### 15.2 安全组最佳实践

```yaml
# 最小开放原则
ingress:
  - description: "允许内部 HTTP"
    from_port: 80
    to_port: 80
    protocol: tcp
    cidr: 10.0.0.0/8
  - description: "允许 SSH 从堡垒机"
    from_port: 22
    to_port: 22
    protocol: tcp
    cidr: 10.0.1.100/32
  # 禁止：0.0.0.0/0 开放所有端口
```

---

## 十六、云审计日志深入

### 16.1 审计日志类型

| 日志类型 | 说明 | 保留建议 |
|----------|------|----------|
| 管理员日志 | API 调用 | 365 天 |
| 数据访问日志 | 数据操作 | 180 天 |
| 系统事件日志 | 系统变更 | 90 天 |
| VPC Flow 日志 | 网络流量 | 30~90 天 |

### 16.2 审计日志分析

```
审计日志分析场景：
  1. 异常登录：非常规 IP/时间登录
  2. 权限提升：IAM 角色变更
  3. 数据泄露：大量数据下载
  4. 配置变更：安全组/密钥变更
  5. 合规审计：谁在何时做了什么

工具：
  CloudTrail + Athena（SQL 分析）
  CloudTrail + CloudWatch Logs Insights
  第三方 SIEM（Splunk/Chronicle）
```

---

## 补充：云安全深度解析

### 1. AWS Security Hub

| 功能 | 说明 |
|------|------|
| 安全评分 | 聚合多安全服务发现 |
| 合规检查 | CIS/PCI DSS/SOC 2 |
| 自动修复 | EventBridge触发自动修复 |
| 跨账号 | 多账号安全聚合 |

### 2. Azure Sentinel

| 功能 | 说明 |
|------|------|
| SIEM | 日志集中分析 |
| SOAR | 安全编排自动化 |
| 威胁情报 | 内置微软威胁情报 |
| 机器学习 | 异常检测 |

### 3. GCP Security Command Center

| 功能 | 说明 |
|------|------|
| 风险分析 | 资产风险评估 |
| 合规检查 | CIS/ISO/SOC |
| 威胁检测 | 异常行为检测 |
| 安全指挥 | 统一安全视图 |

### 4. Cloud SIEM/SOAR

| 工具 | 类型 | 优势 |
|------|------|------|
| Splunk Enterprise Security | SIEM | 功能全面 |
| IBM QRadar | SIEM | 企业级 |
| Chronicle | SIEM | PB级搜索 |
| Swimlane | SOAR | 自动化编排 |

### 5. Cloud Threat Detection

| 检测方式 | 说明 |
|----------|------|
| 基于规则 | 预定义规则匹配 |
| 基于异常 | 机器学习检测异常 |
| 基于威胁情报 | 已知攻击特征 |
| 基于行为 | 用户/实体行为分析 |

### 6. Cloud Security Posture Management

| 功能 | 说明 |
|------|------|
| 资产发现 | 自动发现云资源 |
| 配置评估 | 检查安全配置 |
| 合规检查 | 验证合规状态 |
| 修复建议 | 提供修复方案 |

### 7. Cloud Workload Protection

| 功能 | 说明 |
|------|------|
| 运行时保护 | 容器/VM运行时安全 |
| 漏洞管理 | 已知漏洞扫描 |
| 合规监控 | 工作负载合规 |
| 威胁检测 | 异常行为检测 |

### 8. Cloud Encryption Strategies

| 策略 | 说明 |
|------|------|
| 传输加密 | TLS 1.2+ |
| 存储加密 | AES-256 |
| 数据库加密 | TDE |
| 密钥管理 | KMS/HSM |

### 9. 云安全最佳实践

| 实践 | 说明 |
|------|------|
| 最小权限 | 只授予必要权限 |
| MFA | 强制多因素认证 |
| 加密 | 传输+存储加密 |
| 审计 | 开启操作审计 |
| 监控 | 安全事件监控 |
| 备份 | 定期备份测试 |
| 漏洞扫描 | 定期扫描修复 |
| 安全培训 | 团队安全意识 |

### 10. 云安全架构模式

| 模式 | 说明 |
|------|------|
| 零信任 | 不信任任何网络边界 |
| 微分段 | 细粒度网络隔离 |
| 最小权限 | 默认拒绝 |
| 纵深防御 | 多层安全控制 |

### 11. 云安全工具链

| 工具 | 用途 |
|------|------|
| Trivy | 容器镜像扫描 |
| Checkov | IaC安全扫描 |
| Prowler | AWS安全审计 |
| ScoutSuite | 多云安全审计 |
| Cartography | 资产可视化 |

### 12. 云安全自动化

| 自动化类型 | 说明 |
|------------|------|
| 配置自动修复 | 检测到违规自动修复 |
| 密钥自动轮换 | 定期轮换密钥 |
| 漏洞自动修复 | 自动打补丁 |
| 安全事件响应 | 自动隔离/修复 |

### 13. 云安全合规检查

| 合规框架 | 检查项 |
|----------|--------|
| CIS Benchmark | 基础安全配置 |
| PCI DSS | 支付卡数据安全 |
| HIPAA | 医疗数据隐私 |
| GDPR | 欧盟数据保护 |

### 14. 云安全成本优化

| 策略 | 说明 |
|------|------|
| 免费层利用 | 充分使用免费安全服务 |
| 按需购买 | 按实际需求购买安全服务 |
| 自动化 | 减少人工安全运营成本 |
| 集中化 | 统一安全平台降低成本 |

### 15. 云安全团队职责

| 角色 | 职责 |
|------|------|
| 安全架构师 | 安全架构设计 |
| 安全工程师 | 安全工具部署 |
| 安全运营 | 日常安全监控 |
| 合规专员 | 合规检查报告 |

### 16. 云安全未来趋势

| 趋势 | 说明 |
|------|------|
| AI安全 | AI驱动威胁检测 |
| 零信任架构 | 持续验证 |
| 云原生安全 | 安全左移 |
| 供应链安全 | 软件供应链保护 |

---

## 附录 A：AWS GuardDuty 检测类型与成本控制

### A.1 GuardDuty 检测类型矩阵

| 检测类型 | 数据源 | 检测目标 | 成本影响 |
|----------|--------|----------|----------|
| 异常 API 调用 | CloudTrail | 异常行为、未授权访问 | $1.00/百万事件 |
| VPC Flow 异常 | VPC Flow Logs | 异常网络连接、挖矿流量 | $0.50/GB |
| DNS 异常 | Route 53 DNS Logs | C2 域名通信、DNS 隧道 | $0.50/GB |
| S3 数据泄露 | CloudTrail (S3) | 异常 S3 访问模式 | $1.00/百万事件 |
| EKS 威胁 | EKS Audit Logs | K8s 异常行为、提权 | $1.00/百万事件 |
| EC2 恶意软件 | EBS 快照 | 挖矿/恶意软件检测 | $1.00/百万事件 |
| 容器运行时 | Container Runtime | 容器逃逸、异常进程 | $1.00/百万事件 |

### A.2 成本控制策略

```text
成本控制三板斧：

① 采样频率控制（FindingPublishingFrequency）
   FIFTEEN_MINUTES（默认）→ 成本最低
   ONE_HOUR → 成本再降 4x
   SIX_HOURS → 适合低优先级账号

② CloudTrail 事件过滤
   只监控关键 Service：s3, iam, ec2, eks
   排除高频低风险事件：Describe*/List* 调用
   通过 EventBridge 规则过滤后才写入

③ VPC Flow Logs 采样
   自定义流日志：只记录 ACCEPT/REJECT（丢弃 ALL）
   限制记录端口：只记录 80/443/22 等关键端口
   使用 CloudWatch Logs 采样（降低日志量）
```

```json
{
  "cost_optimization": {
    "finding_publishing_frequency": "FIFTEEN_MINUTES",
    "data_sources": {
      "cloud_trail": {
        "event_sources": ["s3.amazonaws.com", "iam.amazonaws.com"]
      },
      "flow_logs": {
        "status": "ENABLED",
        "log_destination_type": "cloud-watch-logs"
      },
      "dns_logs": {
        "status": "ENABLED"
      }
    }
  }
}
```

## 附录 B：Azure Defender for Cloud 工作负载保护

### B.1 Defender 覆盖范围详解

| 工作负载 | 检测能力 | 定价 |
|----------|----------|------|
| Servers (P1) | VM 漏洞/CVE、恶意软件、异常登录 | $15/月/节点 |
| Servers (P2) | P1 + EDR + 自动响应 | $40/月/节点 |
| Containers | K8s 安全态势、运行时威胁 | $0.001/秒/节点 |
| App Service | Web 应用漏洞、配置检查 | $0.016/小时 |
| SQL Servers | 漏洞、SQL 注入、异常访问 | $15/月/服务器 |
| Storage Accounts | 异常访问、匿名公有访问 | $0.015/小时 |
| Key Vault | 密钥泄露、异常访问 | $0.026/小时 |

### B.2 Defender for Containers 集成流程

```mermaid
flowchart LR
    A[AKS 集群] --> B[Defender Sensor]
    B --> C[Microsoft Defender<br/>云安全中心]
    C --> D[安全建议]
    C --> E[安全事件告警]
    D --> F[自动修复/Policy<br/>Azure Policy]
    E --> G[Logic Apps<br/>自动响应]
```

## 附录 C：GCP Chronicle SIEM 架构

### C.1 Chronicle 架构详解

```
数据流入层：
  ├── 网络设备：VPC Flow / Firewall Logs
  ├── 身份：Cloud Audit / Workspace Audit
  ├── 终端：Endpoint Agent / EDR
  ├── 应用：Syslog / JSON / CEF
  └── 第三方：CrowdStrike / Palo Alto / Okta

处理层：
  ├── 自动解析：结构化/半结构化日志
  ├── IOC 匹配：内置 Google 威胁情报
  ├── YARA 规则：恶意软件检测
  └── SOAR：Playbook 自动响应

查询层：
  ├── UDM 查询：PB 级数据秒级搜索
  ├── SOAR 工作流：自动化事件响应
  └── API：与 SIEM 工具集成
```

### C.2 Chronicle 检测规则示例

```yaml
# YARA-L 检测规则
rule BruteForceLogin {
  meta:
    description = "检测暴力破解登录"
    severity = "HIGH"
    author = "SOC Team"
  events:
    - $e.event_type = "login"
    - $e.status = "failed"
    - $e.src_ip = $src_ip
  condition:
    $e by $src_ip where count(*) > 10 in 5 minutes
  outcome:
    $risk_score = 75
    $mitre_attack = "T1110"
```

## 附录 D：云安全事件响应流程（IR 五阶段）

```mermaid
flowchart TD
    A[1. 准备阶段] --> B[2. 识别阶段]
    B --> C[3. 遏制阶段]
    C --> D[4. 根除阶段]
    D --> E[5. 恢复阶段]
    E --> F[复盘改进]
```

| 阶段 | 任务 | 工具/方法 | SLA |
|------|------|-----------|-----|
| 准备 | 建立 IR 团队、预案文档、Playbook | 安全编排平台 | 年度演练 |
| 识别 | SIEM 告警、威胁情报、IOC 指标匹配 | Chronicle/Splunk | 15 分钟 |
| 遏制 | 隔离受影响资源、封禁恶意 IP | 安全组/VPC 隔离 | 30 分钟 |
| 根除 | 清除恶意软件、修复漏洞、轮换密钥 | EDR/KMS/WAF | 4 小时 |
| 恢复 | 从备份恢复、验证完整性、恢复服务 | 快照/备份 | 8 小时 |

## 附录 E：云合规审计清单（SOC2/HIPAA/PCI-DSS 控制点映射）

| 控制域 | SOC2 | HIPAA | PCI-DSS | 实施工具 |
|--------|------|-------|---------|----------|
| 访问控制 | CC6.1-CC6.3 | §164.312(a) | Req 7/8 | IAM + Permission Boundary |
| 数据加密 | CC6.7 | §164.312(a)(2)(iv) | Req 3/4 | KMS + TLS |
| 审计日志 | CC7.2 | §164.312(b) | Req 10 | CloudTrail + SIEM |
| 漏洞管理 | CC7.1 | §164.308(a)(1) | Req 6 | Inspector + GuardDuty |
| 网络安全 | CC6.6 | §164.312(e) | Req 1/2 | VPC + WAF + DDoS |
| 事件响应 | CC7.3-7.5 | §164.308(a)(6) | Req 12 | SIEM + SOAR |
| 数据备份 | A1.2 | §164.308(a)(7) | Req 9/10 | S3 Cross-Region Replication |
| 物理安全 | — | §164.310 | Req 9 | 云厂商保证 |

## 附录 F：云安全左移工具链（CI/CD 集成）

```mermaid
flowchart LR
    CODE[代码提交] --> SAST[SonarQube<br/>静态代码分析]
    SAST --> SCA[Snyk/Trivy<br/>依赖扫描]
    SCA --> DAST[ZAP/Burp<br/>动态测试]
    DAST --> IAC[Checkov/Terraform<br/>IaC 安全扫描]
    IAC --> CONTAINER[Trivy/Snyk<br/>容器镜像扫描]
    CONTAINER --> DEPLOY[部署]
    DEPLOY --> RUNTIME[Falco/Wiz<br/>运行时保护]
```

| 阶段 | 工具 | 检查内容 | 集成方式 |
|------|------|----------|----------|
| 设计 | Threat Modeling | 威胁建模 | 人工评审 |
| 编码 | SonarQube | 代码质量+安全 | IDE 插件 |
| 构建 | Snyk/Trivy | 依赖漏洞 | CI/CD Pipeline |
| 测试 | OWASP ZAP | DAST 扫描 | CI/CD Pipeline |
| 部署 | Checkov | IaC 安全配置 | CI/CD Pipeline |
| 运行时 | Falco/Wiz | 运行时异常 | K8s Operator |

```yaml
# GitHub Actions CI/CD 安全扫描示例
name: Security Scan
on: [push, pull_request]
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: SAST - SonarQube
        uses: SonarSource/sonarqube-scan-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      
      - name: SCA - Snyk
        uses: snyk/actions@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      
      - name: Container Scan - Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'myapp:${{ github.sha }}'
          format: 'sarif'
          output: 'trivy-results.sarif'
      
      - name: IaC Scan - Checkov
        uses: bridgecrewio/checkov-action@master
        with:
          directory: terraform/
```

## 十七、应用安全 OWASP Top 10 防护

### OWASP Top 10 防护清单

| 风险 | 防护措施 | 实现方式 |
|------|----------|----------|
| 注入攻击 | 参数化查询、ORM | PreparedStatement、MyBatis #{} |
| 身份认证失效 | MFA、JWT 安全存储 | Spring Security + OAuth2 |
| 敏感数据暴露 | 加密存储、传输加密 | AES-256 + TLS 1.3 |
| XXE 攻击 | 禁用外部实体 | XMLParser.setFeature(DISALLOW_DTD) |
| 访问控制失效 | RBAC、最小权限 | Spring Security @PreAuthorize |
| 安全配置错误 | 基线检查、自动化 | CIS Benchmark + ScoutSuite |
| XSS 攻击 | 输出编码、CSP | HTMLUtils.escape() |
| 不安全反序列化 | 白名单校验 | ObjectInputFilter |
| 已知漏洞组件 | SCA 扫描 | Snyk、OWASP Dependency-Check |
| 日志监控不足 | 安全审计日志 | ELK + SIEM |

### IAM 策略与权限模型

```
IAM 策略设计：
  1. 最小权限原则
    - 只授予完成工作所需的最小权限
    - 定期审计权限使用情况

  2. RBAC 模型
    - 角色定义：admin / developer / operator / viewer
    - 角色绑定：用户 → 角色 → 权限
    - 角色继承：高级角色继承低级角色权限

  3. ABAC 模型（细粒度）
    - 属性：用户属性 + 资源属性 + 环境属性
    - 策略：基于属性组合的访问控制
    - 适用：多租户、复杂业务场景

  4. 云 IAM 最佳实践
    - 禁用 root 账号日常操作
    - 启用 MFA
    - 使用临时凭证（STS）
    - 定期轮换密钥
```

### 数据加密与 KMS 管理

| 加密方式 | 加密位置 | 密钥管理 | 适用场景 |
|----------|----------|----------|----------|
| 传输加密 | 网络传输 | TLS 证书 | 所有网络通信 |
| 静态加密 | 存储介质 | KMS 托管密钥 | 数据库/对象存储 |
| 应用层加密 | 应用代码 | 应用管理密钥 | 敏感字段 |
| 字段加密 | 数据库字段 | 列级加密 | 信用卡号/身份证 |

```yaml
# AWS KMS 密钥策略
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "Enable IAM Policies",
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::123456789:root"
      },
      "Action": "kms:*",
      "Resource": "*"
    },
    {
      "Sid": "Allow Use of Key",
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::123456789:role/app-role"
      },
      "Action": [
        "kms:Decrypt",
        "kms:GenerateDataKey"
      ],
      "Resource": "*"
    }
  ]
}
```

### 云审计与合规框架

| 框架 | 核心要求 | 审计频率 | 适用范围 |
|------|----------|----------|----------|
| SOC 2 | 安全/可用性/隐私 | 年度 | SaaS 服务商 |
| ISO 27001 | 信息安全管理 | 年度 | 所有组织 |
| GDPR | 数据隐私保护 | 持续 | 处理欧盟数据 |
| 等保 2.0 | 网络安全等级保护 | 年度 | 国内组织 |
| PCI DSS | 支付卡数据安全 | 年度 | 支付行业 |

```bash
# CloudTrail 审计日志查询
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=ConsoleLogin \
  --start-time 2025-01-01T00:00:00Z \
  --end-time 2025-01-31T23:59:59Z
```

## 十八、云原生应用安全（OWASP Top 10 API Security / SAST/DAST 集成到 CI）

### 18.1 OWASP Top 10 API Security 防护

| API 风险 | 说明 | 防护措施 |
|----------|------|----------|
| BOLA | 对象级授权失效 | 资源级权限校验、UUID 不可预测 |
| Broken Authentication | 认证机制缺陷 | MFA、JWT 过期、密钥轮转 |
| Excessive Data Exposure | 返回过多数据 | 字段级脱敏、GraphQL 深度过滤 |
| Lack of Resources & Rate Limiting | 无速率限制 | API 网关限流、令牌桶 |
| BFLA | 功能级授权失效 | RBAC 策略、最小权限 |
| Mass Assignment | 批量赋值 | DTO 白名单、输入校验 |
| Security Misconfiguration | 安全配置错误 | 基线扫描、自动化加固 |
| Injection | 注入攻击 | 参数化查询、ORM |
| Improper Assets Management | 资产管理不当 | API 目录、废弃接口下线 |
| Insufficient Logging | 日志不足 | 安全审计日志、SIEM 集成 |

### 18.2 SAST/DAST 集成到 CI/CD

```mermaid
flowchart LR
    DEV[代码提交] --> SAST[SonarQube<br/>静态代码分析]
    SAST --> SCA[Snyk/Trivy<br/>依赖扫描]
    SCA --> DAST[ZAP/Burp Suite<br/>动态测试]
    DAST --> IAC[Checkov/Terraform<br/>IaC安全扫描]
    IAC --> CONTAINER[Trivy/Grype<br/>容器镜像扫描]
    CONTAINER --> DEPLOY[部署]
    DEPLOY --> RUNTIME[Falco/Wiz<br/>运行时保护]
```

| 阶段 | 工具 | 检查内容 | 集成方式 |
|------|------|----------|----------|
| 设计 | Threat Modeling | 威胁建模 | 人工评审 |
| 编码 | SonarQube/Semgrep | 代码质量+安全 | IDE 插件 |
| 构建 | Snyk/Trivy | 依赖漏洞 | CI/CD Pipeline |
| 测试 | OWASP ZAP/Burp | DAST 扫描 | CI/CD Pipeline |
| 部署 | Checkov | IaC 安全配置 | CI/CD Pipeline |
| 运行时 | Falco/Wiz | 运行时异常 | K8s Operator |

---

## 十九、IAM 策略深度（最小权限 / 条件键 / 跨账户访问策略）

### 19.1 最小权限策略设计原则

```text
IAM 策略设计四原则：
  1. 最小权限：只授予完成工作所需的最小权限
  2. 按需授权：按工作职责分配角色
  3. 定期审计：检查未使用权限并清理
  4. 临时凭证：用 STS/IRSA 代替长期 AK/SK
```

### 19.2 条件键（Condition Key）高级用法

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowOnlyFromVPC",
      "Effect": "Allow",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::my-bucket/*",
      "Condition": {
        "StringEquals": {
          "aws:sourceVpc": "vpc-12345678"
        }
      }
    },
    {
      "Sid": "DenyNonSSL",
      "Effect": "Deny",
      "Principal": "*",
      "Action": "s3:*",
      "Resource": "arn:aws:s3:::my-bucket/*",
      "Condition": {
        "Bool": {
          "aws:SecureTransport": "false"
        }
      }
    },
    {
      "Sid": "AllowMFAOnly",
      "Effect": "Deny",
      "NotAction": [
        "iam:CreateVirtualMFADevice",
        "iam:EnableMFADevice",
        "iam:ListMFADevices",
        "sts:GetSessionToken"
      ],
      "Resource": "*",
      "Condition": {
        "BoolIfExists": {
          "aws:MultiFactorAuthPresent": "false"
        }
      }
    }
  ]
}
```

### 19.3 跨账户访问策略（AssumeRole）

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::123456789012:root"
      },
      "Action": "sts:AssumeRole",
      "Condition": {
        "StringEquals": {
          "sts:ExternalId": "external-12345"
        },
        "Bool": {
          "aws:MultiFactorAuthPresent": "true"
        }
      }
    }
  ]
}
```

| 条件键 | 说明 | 示例 |
|--------|------|------|
| aws:sourceVpc | 限制来源 VPC | VPC 级别访问控制 |
| aws:sourceVpce | 限制来源 VPC Endpoint | 私网访问 |
| aws:SecureTransport | 是否 HTTPS | 强制加密传输 |
| aws:MultiFactorAuthPresent | 是否启用 MFA | MFA 强制策略 |
| aws:PrincipalOrgID | 限制组织 ID | 跨账户访问控制 |
| sts:AssumeRole | STS 角色切换 | 跨账户访问 |

---

## 二十、数据加密深度（KMS 密钥管理 / 密钥轮转 / 信封加密 / 客户端加密）

### 20.1 KMS 密钥管理最佳实践

| 实践 | 说明 |
|------|------|
| 自动轮转 | CMK 每年自动轮转，旧密钥保留解密能力 |
| 信封加密 | 用 CMK 加密 DEK，DEK 加密实际数据 |
| HSM 保护 | 敏感密钥用 HSM 级别（FIPS 140-2 Level 3） |
| 密钥别名 | 用别名引用，支持无缝轮转 |
| 多区域复制 | 跨区域密钥复制，容灾场景使用 |
| 审计 | 记录所有密钥操作（CloudTrail） |

### 20.2 信封加密流程

```text
信封加密（Envelope Encryption）：
  1. 生成数据密钥 DEK（每次加密用新 DEK）
  2. 用 CMK 加密 DEK → 得到加密的 DEK（CiphertextBlob）
  3. 用 DEK 加密实际数据 → 密文
  4. 存储：加密的 DEK + 密文

  解密流程：
  1. 获取加密的 DEK
  2. 用 CMK 解密 DEK → 明文 DEK
  3. 用 DEK 解密数据

  优势：
    - CMK 不直接接触数据，安全性更高
    - 每次加密用新 DEK，密钥隔离
    - DEK 可以本地缓存，减少 KMS 调用
```

```java
// AWS KMS 信封加密示例
GenerateDataKeyRequest request = new GenerateDataKeyRequest()
    .withKeyId("arn:aws:kms:region:account:key/key-id")
    .withKeySpec("AES_256");

GenerateDataKeyResult result = kmsClient.generateDataKey(request);
byte[] plainKey = result.getPlaintext().asByteArray();
byte[] encryptedKey = result.getCiphertextBlob().asByteArray();

// 用 plainKey 加密数据
byte[] ciphertext = aesEncrypt(plainKey, data);

// 存储 encryptedKey + ciphertext
```

### 20.3 客户端加密（Client-Side Encryption）

```text
客户端加密模式：
  模式1：加密后存储到服务端
    → 服务端只看到密文
    → 需要自己管理密钥

  模式2：服务端辅助加密
    → S3/S3-KMS/S3-Client 等选项
    → 密钥仍由客户端管理

  最佳实践：
    - 敏感字段（密码/身份证）应用层加密
    - 文件/大对象用信封加密
    - 密钥不落库，用环境变量或 KMS
```

---

## 二十一、审计深度（CloudTrail / Azure Activity Log / GCP Audit Log 分析）

### 21.1 CloudTrail 日志分析

```sql
-- 查找未授权 API 调用
SELECT eventTime, eventSource, eventName, userIdentity.arn, sourceIPAddress
FROM cloudtrail_logs
WHERE errorCode = 'AccessDenied'
  AND eventTime > date_add('day', -1, current_timestamp)
ORDER BY eventTime DESC;

-- 查找敏感操作（IAM 变更）
SELECT eventTime, eventName, userIdentity.arn, requestParameters
FROM cloudtrail_logs
WHERE eventSource = 'iam.amazonaws.com'
  AND eventName IN ('CreateUser', 'AttachUserPolicy', 'PutUserPolicy')
  AND eventTime > date_add('day', -7, current_timestamp);

-- 按用户统计 API 调用频率
SELECT userIdentity.arn, COUNT(*) as call_count
FROM cloudtrail_logs
WHERE eventTime > date_add('hour', -1, current_timestamp)
GROUP BY userIdentity.arn
ORDER BY call_count DESC
LIMIT 20;
```

### 21.2 多云审计对比

| 云 | 日志服务 | 保留策略 | 分析工具 |
|----|---------|---------|---------|
| AWS | CloudTrail | 90天（可存S3长期） | Athena/CloudWatch Logs Insights |
| Azure | Activity Log | 90天 | Log Analytics/Microsoft Sentinel |
| GCP | Audit Logs | 400天 | BigQuery/Chronicle |
| 阿里云 | ActionTrail | 365天 | SLS/ODPS |

---

## 二十二、合规框架深度（SOC2 / ISO27001 / HIPAA 在云上实施）

### 22.1 SOC2 实施要点

| 控制域 | 云上实施方式 | 自动化工具 |
|--------|-------------|-----------|
| 安全 | IAM/MFA/加密/网络隔离 | AWS Config/GuardDuty |
| 可用性 | 多AZ/自动扩缩/监控 | CloudWatch/ELB |
| 处理完整性 | 数据校验/事务控制 | DynamoDB/QLDB |
| 机密性 | KMS/加密/脱敏 | Macie/Secrets Manager |
| 隐私 | 数据分类/同意管理 | 隐私工程框架 |

### 22.2 ISO27001 云上控制点

| 控制项 | ISO条款 | 云上实施 |
|--------|---------|---------|
| 信息安全策略 | A.5 | IAM 策略 + 组织策略 |
| 人员安全 | A.6 | MFA + 最小权限 + 审计 |
| 资产管理 | A.8 | 资源标签 + 配置管理 |
| 访问控制 | A.9 | IAM + RBAC + 条件键 |
| 密码学 | A.10 | KMS + TLS + 加密存储 |
| 运营安全 | A.12 | CloudTrail + GuardDuty |
| 通信安全 | A.13 | VPC + WAF + DDoS |
| 系统开发 | A.14 | SAST/DAST + 安全编码 |
| 供应商关系 | A.15 | 责任共担模型 |

### 22.3 HIPAA 合规清单

| 要求 | 云上实施 | 工具 |
|------|---------|------|
| 访问控制 | IAM + MFA | AWS IAM |
| 审计控制 | CloudTrail + 日志保留 | CloudTrail + S3 |
| 传输加密 | TLS 1.2+ | ACM/ALB |
| 存储加密 | AES-256 | KMS + EBS/RDS |
| 数据完整性 | 哈希校验 | S3 Checksum |
| 物理安全 | 云厂商保证 | 合规报告 |
| 事件响应 | 安全事件流程 | Security Hub |

---

## 二十三、CSPM（云安全态势管理工具对比）

| 工具 | 厂商 | 类型 | 关键特性 |
|------|------|------|---------|
| AWS Security Hub | AWS | 原生 | 多安全服务聚合、合规评分 |
| Azure Defender | Azure | 原生 | 威胁检测、合规检查 |
| GCP SCC | GCP | 原生 | 风险分析、威胁检测 |
| Prisma Cloud | Palo Alto | 第三方 | 多云、CI/CD 安全 |
| Wiz | Wiz | 第三方 | Agentless、多云 |
| Prowler | 开源 | 开源 | AWS 安全审计 |
| ScoutSuite | 开源 | 开源 | 多云安全审计 |
| CloudSploit | Aqua | 开源 | 多云配置检查 |

```text
CSPM 核心能力：
  1. 资产发现：自动发现云资源
  2. 配置评估：检查安全配置（CIS Benchmark）
  3. 合规检查：验证合规状态（SOC2/ISO/HIPAA）
  4. 威胁检测：异常行为检测
  5. 修复建议：提供修复方案
  6. 自动修复：触发 EventBridge/Lambda 修复
```

---

## 二十四、云安全中心（威胁检测 / 漏洞扫描 / 配置审计）

### 24.1 威胁检测矩阵

| 检测类型 | AWS GuardDuty | Azure Defender | GCP SCC |
|----------|---------------|----------------|---------|
| 异常 API | 支持 | 支持 | 支持 |
| 挖矿检测 | 支持 | 支持 | 支持 |
| 恶意软件 | 支持 | 支持 | 支持 |
| 数据泄露 | 支持 | 支持 | 支持 |
| 容器安全 | 支持 | 支持 | 支持 |

### 24.2 漏洞扫描对比

| 工具 | 扫描对象 | 集成方式 | 特点 |
|------|---------|---------|------|
| Trivy | 容器镜像/IaC | CI/CD | 开源、轻量 |
| ECR Inspector | ECR 镜像 | 自动 | AWS 原生 |
| Defender for Containers | AKS 容器 | 自动 | Azure 原生 |
| Snyk | 依赖/容器 | CI/CD | SaaS + 开源 |
| Aqua Security | 容器/运行时 | 全周期 | 企业级 |

---

## 二十五、零信任架构（BeyondCorp / ZTNA 在云上的实现）

### 25.1 零信任核心原则

```text
零信任三大原则：
  1. 永不信任，始终验证：所有请求都需验证身份
  2. 最小权限：只授予完成工作所需的最小权限
  3. 假设被入侵：微分段、持续监控、快速响应

BeyondCorp 核心组件：
  身份代理：验证用户身份和设备状态
  访问代理：基于身份+设备+上下文决策
  信任引擎：动态评估风险级别
```

### 25.2 云上零信任实现

| 组件 | AWS | Azure | GCP |
|------|-----|-------|-----|
| 身份管理 | IAM + SSO | Entra ID | Cloud Identity |
| 设备信任 | — | Intune | — |
| 网络微分段 | VPC + Security Group | NSG + Azure Firewall | VPC + Firewall |
| 应用访问 | Verified Access | Application Proxy | IAP |
| 数据安全 | Macie + KMS | Purview + Key Vault | DLP + Cloud KMS |

### 25.3 ZTNA 实现架构

```mermaid
flowchart TB
    USER[用户/设备] -->|身份验证| IDP[身份提供者<br/>Entra ID/IAM]
    IDP -->|信任评估| TRUST[信任引擎<br/>设备状态/位置/风险]
    TRUST -->|允许| PROXY[访问代理<br/>ZTNA Gateway]
    TRUST -->|拒绝| DENY[拒绝访问]
    PROXY -->|mTLS| APP[应用/服务]
    APP -->|审计| AUDIT[审计日志<br/>SIEM/日志分析]
```

### 25.4 零信任 vs 传统边界安全

| 维度 | 传统边界安全 | 零信任架构 |
|------|-------------|-----------|
| 信任模型 | 内网可信，外网不可信 | 内外网均不可信 |
| 验证方式 | 一次验证，长期有效 | 持续验证 |
| 访问控制 | 网络边界控制 | 身份+设备+上下文 |
| 微分段 | 粗粒度（VLAN） | 细粒度（应用/接口级） |
| 监控 | 网络流量监控 | 全链路审计 |
| 适用场景 | 传统企业 | 混合云/远程办公 |

---

## 二十六、与其他板块的关系

- 认证授权原理见「[认证授权 JWT/OAuth2](./认证授权JWT-OAuth2.md)」；
- 云 IAM 体系见「[云身份与访问管理体系](./云身份与访问管理体系.md)」；
- 网络安全（TLS/加密）见「[安全工程/常见Web安全漏洞与防护](../../安全工程/03-常见Web安全漏洞与防护.md)」；
- 等保合规见「[安全工程/数据安全与密码学基础](../../安全工程/05-数据安全与密码学基础.md)」。

---

## 十、云安全最佳实践清单

### 10.1 身份与访问

| 实践 | 说明 |
|------|------|
| 最小权限 | 只授予完成工作所需的最小权限 |
| MFA | 所有用户强制多因素认证 |
| 临时凭证 | 用 STS/IRSA，避免长期 AK/SK |
| 密钥轮换 | 定期轮换（90天） |

### 10.2 网络安全

| 实践 | 说明 |
|------|------|
| 安全组 | 最小开放原则（只开必要端口） |
| VPC 隔离 | 不同环境/租户用独立 VPC |
| WAF | 应用层防护（SQL注入/XSS/CC） |
| DDoS | 基础防护 + 高防套餐 |

### 10.3 数据安全

| 实践 | 说明 |
|------|------|
| 传输加密 | TLS 1.2+（禁用 SSLv3） |
| 存储加密 | KMS 信封加密 |
| 备份 | 自动备份 + 跨区域复制 |
| 脱敏 | 测试环境数据脱敏 |

### 10.4 审计与合规

| 实践 | 说明 |
|------|------|
| 操作审计 | CloudTrail/ActionTrail 必开 |
| 日志保留 | 至少 180 天（等保要求） |
| 定期扫描 | 漏洞扫描 + 基线核查 |
| 合规报告 | 等保/SOC2/GDPR 定期生成 |

---

## 十一、安全事件响应流程

```
1. 检测：云安全中心/CloudTrail 告警
2. 分析：确认事件范围与影响
3. 遏制：隔离受影响资源（安全组/网络策略）
4. 根除：修复漏洞/清除恶意软件
5. 恢复：从备份恢复/重建资源
6. 复盘：事件报告 + 改进措施
```

---

## 十二、WAF 规则配置示例

### 12.1 AWS WAF 规则

```json
{
  "Rules": [
    {
      "Priority": 1,
      "Action": { "Type": "BLOCK" },
      "Statement": {
        "SqliMatchStatement": {
          "FieldToMatch": { "Body": {} },
          "TextTransformations": [{ "Priority": 0, "Type": "URL_DECODE" }]
        }
      }
    },
    {
      "Priority": 2,
      "Action": { "Type": "BLOCK" },
      "Statement": {
        "XssMatchStatement": {
          "FieldToMatch": { "QueryString": {} },
          "TextTransformations": [{ "Priority": 0, "Type": "NONE" }]
        }
      }
    }
  ]
}
```

### 12.2 DDoS 防护配置

| 层级 | 防护方式 | 阈值 |
|------|----------|------|
| L3/4 | Anycast 流量清洗 | Tbps 级 |
| L7 | WAF + 速率限制 | 1000 QPS/IP |
| 应用层 | 验证码/人机识别 | 按业务调整 |

---

## 十三、合规检查清单

| 检查项 | 等保要求 | 实施方式 |
|--------|----------|----------|
| 身份鉴别 | 双因素认证 | MFA + 密码策略 |
| 访问控制 | 最小权限 | IAM + 权限边界 |
| 安全审计 | 操作审计 | CloudTrail/ActionTrail |
| 数据加密 | 传输+存储加密 | TLS + KMS |
| 入侵防范 | WAF + DDoS | 云安全服务 |
| 数据备份 | 定期备份 | 自动备份 + 恢复演练 |

---

## 十四、云安全服务价格参考

| 服务 | 免费额度 | 付费价格 |
|------|----------|----------|
| AWS WAF | — | $0.60/百万请求 |
| AWS Shield Standard | 免费 | — |
| AWS Shield Advanced | — | $3,000/月 + 数据费 |
| 阿里云 WAF | — | ¥3,000/年起 |
| 阿里云 DDoS 高防 | — | ¥20,000/年起 |
| AWS KMS | 20,000 次免费 | $0.03/10,000 次 |

---

## 十五、云安全自动化工具

| 工具 | 用途 |
|------|------|
| AWS Config Rules | 资源配置合规检查 |
| CloudFormation Guard | IaC 模板安全校验 |
| Trivy | 容器镜像漏洞扫描 |
| Checkov | 基础设施代码安全扫描 |
| Prowler | AWS 安全审计工具 |

---

## 十六、GuardDuty/Azure Defender 云威胁检测

### 16.1 威胁检测能力

| 检测类型 | AWS GuardDuty | Azure Defender | 说明 |
|----------|---------------|----------------|------|
| 异常API调用 | 支持 | 支持 | 识别异常行为 |
| 挖矿检测 | 支持 | 支持 | 识别加密挖矿 |
| 恶意软件 | 支持 | 支持 | 文件/网络恶意软件 |
| 数据泄露 | 支持 | 支持 | 敏感数据外泄 |
| 容器安全 | 支持 | 支持 | 容器异常行为 |

### 16.2 配置示例

```yaml
# AWS GuardDuty 配置
Resources:
  GuardDutyDetector:
    Type: AWS::GuardDuty::Detector
    Properties:
      Enable: true
      FindingPublishingFrequency: FIFTEEN_MINUTES
      DataSources:
        S3Logs:
          Enable: true
        Kubernetes:
          AuditLogs:
            Enable: true
        CloudTrail:
          Logs:
            Enable: true
```

## 十七、Chronicle SIEM 安全分析

### 17.1 安全分析流程

```mermaid
flowchart LR
    COLLECT[日志收集] --> ENRICH[数据富化]
    ENRICH --> DETECT[威胁检测]
    DETECT --> INVESTIGATE[调查分析]
    INVESTIGATE --> RESPONSE[响应处置]
```

### 17.2 检测规则示例

```yaml
# Chronicle 检测规则
rule BruteForceLogin {
  meta:
    description = "检测暴力破解登录"
    severity = "HIGH"
  events:
    - $e.event_type = "login"
    - $e.status = "failed"
  condition:
    $e by $e.src_ip where count(*) > 10 in 5 minutes
}
```

## 十八、Incident Response 流程

| 阶段 | 任务 | 工具 |
|------|------|------|
| 准备 | 建立响应团队、预案 | Playbook |
| 检测 | 发现安全事件 | SIEM/EDR |
| 分析 | 确定影响范围 | 日志分析 |
| 遏制 | 控制事件扩散 | 隔离/封禁 |
| 清除 | 消除威胁 | 杀毒/修复 |
| 恢复 | 恢复正常运行 | 备份/重建 |
| 总结 | 复盘改进 | 报告 |

## 十九、合规清单与自动化

### 19.1 常见合规要求

| 合规标准 | 要求 | 自动化工具 |
|----------|------|-----------|
| 等保2.0 | 身份认证/访问控制/安全审计 | AWS Config |
| GDPR | 数据加密/删除权/隐私保护 | AWS Macie |
| PCI DSS | 支付数据加密/网络隔离 | AWS WAF |
| SOC 2 | 安全/可用性/处理完整性 | AWS Security Hub |

### 19.2 合规自动化检查

```yaml
# AWS Config 合规规则
Resources:
  RootAccessRule:
    Type: AWS::Config::ConfigRule
    Properties:
      ConfigRuleName: root-access-check
      Source:
        Owner: AWS
        SourceIdentifier: ROOT_ACCESS_CHECK
```

## 二十、左移安全工具链

| 阶段 | 工具 | 用途 |
|------|------|------|
| 设计 | Threat Modeling | 威胁建模 |
| 编码 | SonarQube | 代码质量+安全 |
| 构建 | Trivy/Snyk | 依赖扫描 |
| 测试 | OWASP ZAP | DAST扫描 |
| 部署 | Checkov/IaC扫描 | 配置安全 |
| 运行时 | Falco/Wiz | 运行时保护 |

```mermaid
flowchart LR
    DESIGN[设计] --> CODE[编码]
    CODE --> BUILD[构建]
    BUILD --> TEST[测试]
    TEST --> DEPLOY[部署]
    DEPLOY --> RUN[运行时]
    DESIGN -->|Threat Modeling| SEC1[安全设计]
    CODE -->|SonarQube| SEC2[代码安全]
    BUILD -->|Trivy| SEC3[依赖安全]
    TEST -->|ZAP| SEC4[动态安全]
    DEPLOY -->|Checkov| SEC5[配置安全]
    RUN -->|Falco| SEC6[运行时安全]
```

---

## WAF 规则配置实战

```json
// AWS WAF 规则配置
{
  "Rules": [
    {
      "Name": "RateLimit",
      "Priority": 1,
      "Action": { "Block": {} },
      "Statement": {
        "RateBasedStatement": {
          "Limit": 2000,
          "AggregateKeyType": "IP"
        }
      }
    },
    {
      "Name": "SQLInjection",
      "Priority": 2,
      "Action": { "Block": {} },
      "Statement": {
        "SqliMatchStatement": {
          "FieldToMatch": { "Body": {} },
          "TextTransformations": [
            { "Priority": 0, "Type": "URL_DECODE" }
          ]
        }
      }
    },
    {
      "Name": "XSS",
      "Priority": 3,
      "Action": { "Block": {} },
      "Statement": {
        "XssMatchStatement": {
          "FieldToMatch": { "Body": {} },
          "TextTransformations": [
            { "Priority": 0, "Type": "URL_DECODE" }
          ]
        }
      }
    }
  ]
}
```

### WAF 规则优先级

| 优先级 | 规则类型 | 说明 | 处理方式 |
|--------|----------|------|----------|
| 1 | 速率限制 | 防DDoS/CC | 封禁IP |
| 2 | SQL注入 | 防数据库攻击 | 封禁请求 |
| 3 | XSS | 防跨站脚本 | 封禁请求 |
| 4 | 路径限制 | 保护敏感路径 | 封禁请求 |
| 5 | 自定义规则 | 业务逻辑 | 按需处理 |

## DDoS 防护配置

```yaml
# AWS Shield Advanced 配置
Protection:
  - Name: "Web应用防护"
    ResourceArn: "arn:aws:elasticloadbalancing:us-east-1:123456789:loadbalancer/app/my-alb/abc123"
    ProtectionLimits:
      - Type: " ShieldFixedRateLimit"
        Limit: 2000  # 每秒请求限制
      - Type: " ShieldPacedRateLimit"  
        Limit: 100   # 突发限制

# 自动缓解
ShieldAdvancedProtection:
  - ResourceArn: "arn:aws:ec2:us-east-1:123456789:instance/i-abc123"
    AutoMitigation: true
```

### DDoS 防护策略

| 策略 | 说明 | 适用攻击 |
|------|------|----------|
| Anycast | 全球清洗 | 大流量攻击 |
| 速率限制 | 请求限速 | CC攻击 |
| 地理封锁 | 按地区封锁 | 地域攻击 |
| 行为分析 | ML检测异常 | 复杂攻击 |

## 合规审计清单

```yaml
# 合规检查配置
ComplianceChecks:
  - Name: "等保三级"
    Checks:
      - "访问控制: 身份认证"
      - "访问控制: 权限管理"
      - "安全审计: 日志记录"
      - "通信安全: 传输加密"
      - "数据安全: 存储加密"
    
  - Name: "GDPR"
    Checks:
      - "数据最小化"
      - "用户同意"
      - "数据可移植性"
      - "被遗忘权"
      - "数据泄露通知"
```

### 合规要求对比

| 合规要求 | 要点 | 实施难度 | 优先级 |
|----------|------|----------|--------|
| 等保三级 | 身份认证、访问控制、审计 | 高 | 高 |
| GDPR | 用户数据保护 | 中 | 高 |
| PCI DSS | 支付数据安全 | 高 | 中 |
| SOC 2 | 可用性、安全性、完整性 | 中 | 中 |

## 安全运维流程

```mermaid
flowchart TB
    subgraph 日常运维
        MONITOR[监控告警] --> TRIAGE[分类处理]
        TRIAGE --> INVESTIGATE[调查分析]
        INVESTIGATE --> REMEDIATE[修复处置]
        REMEDIATE --> REVIEW[复盘改进]
    end
    subgraph 应急响应
        DETECT[事件检测] --> CONTAIN[遏制]
        CONTAIN --> ERADICATE[根除]
        ERADICATE --> RECOVER[恢复]
        RECOVER --> LESSON[经验总结]
    end
```

### 安全运维SOP

| 步骤 | 操作 | 时间要求 | 责任人 |
|------|------|----------|--------|
| 1 | 告警接收 | 5分钟 | 值班人员 |
| 2 | 事件分类 | 15分钟 | 安全工程师 |
| 3 | 影响评估 | 30分钟 | 安全工程师 |
| 4 | 处置执行 | 1小时 | 运维工程师 |
| 5 | 复盘报告 | 24小时 | 安全团队 |

## 云安全合规框架

### 合规标准对比

| 标准 | 范围 | 适用行业 | 认证周期 | 关键要求 |
|------|------|----------|----------|----------|
| ISO 27001 | 信息安全管理体系 | 通用 | 3年 | ISMS建设 |
| SOC 2 | 服务商安全控制 | SaaS/云服务 | 6个月 | 5大信任原则 |
| PCI DSS | 支付卡数据安全 | 金融/支付 | 1年 | 12项要求 |
| 等保2.0 | 网络安全等级保护 | 国内通用 | 1年 | 5个级别 |
| GDPR | 个人数据保护 | 欧盟业务 | 持续 | 数据主体权利 |

### 云安全合规检查清单

```yaml
# 云安全合规检查配置
security_compliance:
  # 身份与访问管理
  iam:
    - name: "最小权限原则"
      check: "IAM策略是否遵循最小权限"
      severity: "高"
    - name: "MFA强制"
      check: "管理员账户是否启用MFA"
      severity: "高"
    - name: "密钥轮换"
      check: "密钥是否定期轮换"
      severity: "中"
  
  # 数据保护
  data_protection:
    - name: "加密传输"
      check: "是否强制HTTPS/TLS"
      severity: "高"
    - name: "加密存储"
      check: "敏感数据是否加密存储"
      severity: "高"
    - name: "备份策略"
      check: "数据备份是否完整"
      severity: "中"
  
  # 网络安全
  network:
    - name: "网络隔离"
      check: "VPC/子网划分是否合理"
      severity: "高"
    - name: "安全组"
      check: "安全组规则是否严格"
      severity: "中"
    - name: "WAF部署"
      check: "Web应用是否启用WAF"
      severity: "中"
  
  # 监控审计
  monitoring:
    - name: "日志记录"
      check: "操作日志是否完整"
      severity: "高"
    - name: "告警配置"
      check: "安全事件是否配置告警"
      severity: "中"
    - name: "定期审计"
      check: "是否定期进行安全审计"
      severity: "中"
```

### 云安全态势管理（CSPM）

```mermaid
graph TB
    subgraph 数据采集
        CLOUD[云平台API]
        AGENT[安全Agent]
        LOG[日志流]
    end
    subgraph 分析引擎
        RULE[规则引擎]
        ML[机器学习]
        ANOMALY[异常检测]
    end
    subgraph 响应处置
        ALERT[告警通知]
        AUTO[自动修复]
        REPORT[合规报告]
    end
    CLOUD --> RULE
    AGENT --> ML
    LOG --> ANOMALY
    RULE --> ALERT
    ML --> AUTO
    ANOMALY --> REPORT
```

---

## 应急响应预案

```yaml
# 应急响应预案配置
IncidentResponse:
  - Level: "P1-严重"
    Scenario: "数据泄露"
    ResponseTime: "15分钟"
    Actions:
      - "隔离受影响系统"
      - "通知管理层"
      - "启动取证调查"
      - "通知监管机构"
    
  - Level: "P2-高"
    Scenario: "服务中断"
    ResponseTime: "30分钟"
    Actions:
      - "切换备用系统"
      - "排查故障原因"
      - "恢复服务"
    
  - Level: "P3-中"
    Scenario: "漏洞利用"
    ResponseTime: "2小时"
    Actions:
      - "评估影响范围"
      - "修补漏洞"
      - "加强监控"
```

### 应急联系人

## 云安全模型（共享责任详解）

### 共享责任模型

```text
共享责任模型：
  云提供商责任（Security OF the Cloud）：
    ├── 物理安全
    ├── 网络基础设施
    ├── 计算虚拟化
    ├── 存储基础设施
    └── 数据库服务

  客户责任（Security IN the Cloud）：
    ├── 数据安全
    ├── 身份与访问管理
    ├── 应用安全
    ├── 操作系统配置
    └── 网络配置
```

### 各云厂商责任划分

| 责任项 | IaaS | PaaS | SaaS |
|--------|------|------|------|
| 物理安全 | 云厂商 | 云厂商 | 云厂商 |
| 网络安全 | 共同 | 云厂商 | 云厂商 |
| 主机安全 | 客户 | 云厂商 | 云厂商 |
| 应用安全 | 客户 | 客户 | 云厂商 |
| 数据安全 | 客户 | 客户 | 客户 |
| 身份管理 | 客户 | 客户 | 客户 |

---

## 网络安全（VPC/NACL/WAF）

### VPC安全架构

```mermaid
graph TB
    subgraph VPC
        subgraph 公有子网
            ALB[负载均衡器]
            NAT[NAT网关]
        end
        subgraph 私有子网
            App[应用服务器]
            DB[数据库]
        end
    end
    subgraph 安全组
        SG1[Web安全组]
        SG2[App安全组]
        SG3[DB安全组]
    end
    Internet --> ALB
    ALB --> App
    App --> DB
    SG1 --> ALB
    SG2 --> App
    SG3 --> DB
```

### 安全组规则配置

```yaml
# AWS安全组配置
security_groups:
  web-sg:
    ingress:
      - protocol: tcp
        port: 443
        source: 0.0.0.0/0
        description: "HTTPS访问"
    egress:
      - protocol: tcp
        port: 8080
        destination: app-sg
        description: "转发到应用层"
  
  app-sg:
    ingress:
      - protocol: tcp
        port: 8080
        source: web-sg
        description: "接收Web层请求"
    egress:
      - protocol: tcp
        port: 3306
        destination: db-sg
        description: "访问数据库"
  
  db-sg:
    ingress:
      - protocol: tcp
        port: 3306
        source: app-sg
        description: "接收应用层连接"
```

### WAF配置

```yaml
# WAF规则配置
waf_rules:
  # SQL注入防护
  - name: "SQL注入防护"
    action: "block"
    priority: 1
    conditions:
      - type: "uri"
        operator: "contains"
        values: ["union", "select", "insert", "delete"]
  
  # XSS防护
  - name: "XSS防护"
    action: "block"
    priority: 2
    conditions:
      - type: "body"
        operator: "contains"
        values: ["<script>", "javascript:", "onerror"]
  
  # 路径遍历防护
  - name: "路径遍历防护"
    action: "block"
    priority: 3
    conditions:
      - type: "uri"
        operator: "contains"
        values: ["../", "..\\"]
```

---

## 数据安全（加密/密钥管理/脱敏）

### 数据加密策略

```text
数据加密策略：
  1. 传输加密：TLS 1.2+
  2. 存储加密：AES-256
  3. 密钥管理：KMS/HSM
  4. 数据脱敏：敏感字段脱敏
```

### 密钥管理架构

```mermaid
graph TB
    subgraph 应用层
        App[应用程序]
    end
    subgraph 密钥管理
        KMS[云KMS]
        HSM[硬件HSM]
    end
    subgraph 数据层
        DB[加密数据库]
        S3[加密存储]
    end
    App --> KMS
    App --> HSM
    KMS --> DB
    KMS --> S3
    HSM --> DB
    HSM --> S3
```

### 数据脱敏规则

| 数据类型 | 脱敏规则 | 示例 |
|----------|----------|------|
| 手机号 | 保留前3后4 | 138****1234 |
| 身份证 | 保留前4后3 | 1101****123 |
| 银行卡 | 保留后4 | ****1234 |
| 邮箱 | 保留前3 | zha***@example.com |
| 地址 | 保留省市 | 北京市朝阳区*** |

---

## 身份安全（IAM/MFA/SSO）

### IAM最佳实践

```text
IAM最佳实践：
  1. 最小权限原则：只授予必要权限
  2. 职责分离：关键操作需要多人审批
  3. 定期审查：定期审查权限配置
  4. MFA启用：强制多因素认证
  5. 密钥轮转：定期轮转访问密钥
```

### MFA配置

```yaml
# MFA配置
mfa_configuration:
  # MFA类型
  type: "TOTP"  # Time-based One-Time Password
  
  # 强制MFA
  enforce_for:
    - "root_user"
    - "admin_user"
    - "privileged_user"
  
  # MFA设备管理
  device_management:
    max_devices: 3
    backup_codes: 10
    recovery_period: "30d"
```

### SSO架构

```mermaid
graph TB
    subgraph 身份提供商
        IdP[身份提供商]
    end
    subgraph 服务提供商
        SP1[应用1]
        SP2[应用2]
        SP3[应用3]
    end
    subgraph 用户
        User[用户]
    end
    User --> IdP
    IdP --> User
    User --> SP1
    SP1 --> IdP
    User --> SP2
    SP2 --> IdP
    User --> SP3
    SP3 --> IdP
```

---

## 合规（等保/GDPR/SOC2）

### 等保2.0要求

| 等级 | 安全要求 | 适用场景 |
|------|----------|----------|
| 一级 | 基本保护 | 一般系统 |
| 二级 | 保护重要信息 | 重要系统 |
| 三级 | 保护重要信息 | 关键系统 |
| 四级 | 保护核心信息 | 核心系统 |

### GDPR合规要求

```text
GDPR核心要求：
  1. 数据主体权利：访问权、删除权、可携带权
  2. 数据处理原则：合法、公正、透明
  3. 数据保护设计：隐私设计、默认隐私
  4. 数据保护影响评估：高风险处理前评估
  5. 数据泄露通知：72小时内通知监管机构
```

### SOC2合规框架

| 信任原则 | 安全要求 | 控制措施 |
|----------|----------|----------|
| 安全 | 系统防护 | 访问控制、加密 |
| 可用性 | 系统可用 | 容灾、监控 |
| 处理完整性 | 系统处理完整 | 错误检查 |
| 保密性 | 信息保密 | 加密、脱敏 |
| 隐私 | 个人数据保护 | 隐私政策 |

---

## 安全运营（SIEM/SOC）

### SIEM架构

```mermaid
graph TB
    subgraph 数据源
        Log[日志]
        Net[网络流量]
        Endpoint[终端]
    end
    subgraph SIEM
        Collect[数据采集]
        Normalize[数据标准化]
        Correlate[关联分析]
        Alert[告警]
    end
    subgraph SOC
        Analyst[安全分析师]
        Playbook[剧本]
        Response[响应]
    end
    Log --> Collect
    Net --> Collect
    Endpoint --> Collect
    Collect --> Normalize
    Normalize --> Correlate
    Correlate --> Alert
    Alert --> Analyst
    Analyst --> Playbook
    Playbook --> Response
```

### 安全事件处理流程

```yaml
# 安全事件处理流程
incident_response:
  # 事件分级
  levels:
    - level: "P1-严重"
      response_time: "15分钟"
      actions:
        - "隔离受影响系统"
        - "通知管理层"
        - "启动取证调查"
    
    - level: "P2-高"
      response_time: "30分钟"
      actions:
        - "限制影响范围"
        - "排查故障原因"
        - "恢复服务"
    
    - level: "P3-中"
      response_time: "2小时"
      actions:
        - "评估影响范围"
        - "修补漏洞"
        - "加强监控"
```

---

## 容器安全

### 容器安全架构

```text
容器安全层次：
  1. 镜像安全：漏洞扫描、签名验证
  2. 运行时安全：行为监控、入侵检测
  3. 编排安全：RBAC、网络策略
  4. 存储安全：加密、访问控制
```

### Kubernetes安全配置

```yaml
# Pod安全策略
apiVersion: policy/v1beta1
kind: PodSecurityPolicy
metadata:
  name: restricted
spec:
  privileged: false
  allowPrivilegeEscalation: false
  requiredDropCapabilities:
    - ALL
  volumes:
    - 'configMap'
    - 'emptyDir'
    - 'persistentVolumeClaim'
  hostNetwork: false
  hostIPC: false
  hostPID: false
  runAsUser:
    rule: 'MustRunAsNonRoot'
  seLinux:
    rule: 'RunAsAny'
  fsGroup:
    rule: 'RunAsAny'
  supplementalGroups:
    rule: 'RunAsAny'
```

### 镜像扫描配置

```yaml
# Trivy镜像扫描配置
scan_config:
  # 扫描目标
  targets:
    - "container_image"
    - "filesystem"
    - "repository"
  
  # 漏洞严重级别
  severity_threshold: "HIGH"
  
  # 忽略未修复漏洞
  ignore_unfixed: true
  
  # 扫描超时
  timeout: "5m"
```

---

## 安全最佳实践

### 安全基线配置

| 安全领域 | 基线配置 | 检查频率 |
|----------|----------|----------|
| 身份认证 | 强密码策略+MFA | 每月 |
| 访问控制 | 最小权限原则 | 每周 |
| 网络安全 | 防火墙+WAF | 每日 |
| 数据加密 | 传输加密+存储加密 | 每月 |
| 日志审计 | 操作日志完整记录 | 每日 |
| 漏洞管理 | 定期扫描+及时修复 | 每周 |

### 安全检查清单

```text
安全检查清单：
  ✓ 身份认证
    ├── 是否启用MFA
    ├── 密码策略是否符合要求
    └── 是否定期轮转密钥
  
  ✓ 访问控制
    ├── 是否遵循最小权限原则
    ├── 是否定期审查权限
    └── 是否禁用默认账户
  
  ✓ 网络安全
    ├── 是否配置安全组
    ├── 是否启用WAF
    └── 是否限制公网访问
  
  ✓ 数据安全
    ├── 是否加密敏感数据
    ├── 是否配置密钥管理
    └── 是否实施数据脱敏
  
  ✓ 监控审计
    ├── 是否启用安全监控
    ├── 是否记录操作日志
    └── 是否配置告警规则
```

---

## 安全评估

### 安全评估方法

| 方法 | 说明 | 适用场景 |
|------|------|----------|
| 渗透测试 | 模拟攻击测试 | 定期评估 |
| 漏洞扫描 | 自动化漏洞检测 | 持续监控 |
| 安全审计 | 合规性检查 | 合规要求 |
| 红蓝对抗 | 攻防演练 | 高安全要求 |

### 安全评估工具

```yaml
# 安全评估工具
security_tools:
  # 漏洞扫描
  vulnerability_scanning:
    - name: "Nessus"
      type: "网络漏洞扫描"
    - name: "OpenVAS"
      type: "开源漏洞扫描"
  
  # 渗透测试
  penetration_testing:
    - name: "Metasploit"
      type: "渗透测试框架"
    - name: "Burp Suite"
      type: "Web应用测试"
  
  # 安全审计
  security_audit:
    - name: "Lynis"
      type: "系统安全审计"
    - name: "ScoutSuite"
      type: "云安全审计"
```

---

## 安全应急

### 应急响应预案

```yaml
# 应急响应预案
incident_response:
  - Level: "P1-严重"
    Scenario: "数据泄露"
    ResponseTime: "15分钟"
    Actions:
      - "隔离受影响系统"
      - "通知管理层"
      - "启动取证调查"
      - "通知监管机构"
  
  - Level: "P2-高"
    Scenario: "服务中断"
    ResponseTime: "30分钟"
    Actions:
      - "切换备用系统"
      - "排查故障原因"
      - "恢复服务"
  
  - Level: "P3-中"
    Scenario: "漏洞利用"
    ResponseTime: "2小时"
    Actions:
      - "评估影响范围"
      - "修补漏洞"
      - "加强监控"
```

### 应急联系人

| 角色 | 职责 | 联系方式 |
|------|------|----------|
| 安全负责人 | 总体协调 | 手机+邮箱 |
| 运维负责人 | 系统恢复 | 手机+邮箱 |
| 开发负责人 | 代码修复 | 手机+邮箱 |
| 法务负责人 | 合规处理 | 手机+邮箱 |
| 公关负责人 | 对外沟通 | 手机+邮箱 |

---

## 云安全高级实践与故障排查

### 云原生应用安全

```yaml
# 云原生应用安全配置
cloud_native_security:
  # 容器安全
  container_security:
    image_scanning: true
    runtime_protection: true
    secrets_management: true
    network_policies: true
  
  # K8s安全
  kubernetes_security:
    pod_security: "restricted"
    rbac: "enabled"
    network_policies: "enabled"
    secrets_encryption: "enabled"
  
  # 服务网格安全
  service_mesh_security:
    mtls: "strict"
    authorization_policies: "enabled"
    telemetry: "enabled"
  
  # 代码安全
  code_security:
    sast: "enabled"
    dast: "enabled"
    sca: "enabled"
    secret_scanning: "enabled"

# 安全策略
security_policies:
  # 镜像策略
  image_policy:
    - "只使用可信镜像"
    - "扫描已知漏洞"
    - "签名验证"
    - "最小基础镜像"
  
  # 运行时策略
  runtime_policy:
    - "只读文件系统"
    - "非Root运行"
    - "资源限制"
    - "网络策略"
  
  # 数据策略
  data_policy:
    - "加密存储"
    - "访问控制"
    - "审计日志"
    - "数据脱敏"
```

| 安全层面 | 说明 | 重要性 |
|----------|------|--------|
| 容器安全 | 镜像扫描+运行时保护 | 高 |
| K8s安全 | Pod安全+RBAC | 高 |
| 服务网格 | mTLS+授权策略 | 高 |
| 代码安全 | SAST/DAST/SCA | 高 |

### IAM策略高级

```yaml
# IAM策略配置
iam_policies:
  # 最小权限策略
  least_privilege:
    - "只授予必要权限"
    - "使用条件键限制"
    - "定期审查权限"
    - "删除未使用权限**
  
  # 临时凭证策略
  temporary_credentials:
    - "使用STS获取临时凭证"
    - "设置合理的过期时间"
    - "避免长期AK/SK"
    - "使用角色替代用户**
  
  # 多因素认证策略
  mfa_strategy:
    - "强制启用MFA"
    - "使用硬件MFA设备"
    - "设置MFA策略"
    - "定期更换MFA设备**
  
  # 联邦认证策略
  federation_strategy:
    - "使用OIDC/SAML联邦"
    - "验证身份提供者"
    - "检查断言签名"
    - "验证受众限制"
```

| IAM策略 | 说明 | 重要性 |
|---------|------|--------|
| 最小权限 | 只授予必要权限 | 高 |
| 临时凭证 | 使用短期凭证 | 高 |
| 多因素认证 | 多重身份验证 | 高 |
| 联邦认证 | 统一身份管理 | 高 |

### 数据加密

```yaml
# 数据加密配置
data_encryption:
  # 静态加密
  at_rest_encryption:
    enabled: true
    algorithm: "AES-256"
    key_management: "KMS"
  
  # 传输加密
  in_transit_encryption:
    enabled: true
    protocol: "TLS 1.3"
    certificate_management: "ACM"
  
  # 应用层加密
  application_layer_encryption:
    enabled: true
    algorithm: "RSA-2048"
    key_rotation: "annual"
  
  # 加密策略
  encryption_policies:
    - "所有数据必须加密"
    - "密钥定期轮换"
    - "使用托管密钥服务"
    - "监控密钥使用"
```

| 加密类型 | 说明 | 适用场景 |
|----------|------|----------|
| 静态加密 | 存储加密 | 数据存储 |
| 传输加密 | 网络加密 | 数据传输 |
| 应用层加密 | 业务加密 | 敏感数据 |

### 审计与合规

```yaml
# 审计配置
audit_configuration:
  # 日志审计
  log_audit:
    enabled: true
    retention: "90 days"
    encryption: true
  
  # 操作审计
  operation_audit:
    enabled: true
    events:
      - "CreateUser"
      - "DeleteUser"
      - "AttachPolicy"
      - "DetachPolicy"
  
  # 合规框架
  compliance_frameworks:
    - name: "SOC2"
      requirements:
        - "访问控制"
        - "审计日志"
        - "密钥管理"
        - "安全监控"
    
    - name: "ISO27001"
      requirements:
        - "身份管理"
        - "访问控制"
        - "密码管理"
        - "安全审计"
    
    - name: "GDPR"
      requirements:
        - "数据最小化"
        - "访问控制"
        - "审计追踪"
        - "数据保护"
  
  # 合规检查
  compliance_checks:
    - name: "未使用权限"
      query: "SELECT user_name, policy_name FROM iam_user_policies WHERE last_used < 90 days ago"
      action: "删除未使用权限"
    
    - name: "过度权限"
      query: "SELECT user_name, policy_name FROM iam_user_policies WHERE policy_action = '*'"
      action: "限制权限范围"
    
    - name: "密钥过期"
      query: "SELECT user_name, access_key_id FROM iam_access_keys WHERE status = 'Active' AND create_date < 90 days ago"
      action: "轮换密钥"
```

| 审计维度 | 说明 | 合规要求 |
|----------|------|----------|
| 日志审计 | 操作日志 | SOC2/ISO27001 |
| 操作审计 | API调用 | SOC2/GDPR |
| 合规框架 | 合规要求 | 多种框架 |
| 合规检查 | 自动检查 | 持续合规 |

### CSPM（云安全态势管理）

```yaml
# CSPM配置
cspm_configuration:
  # 资源发现
  resource_discovery:
    enabled: true
    frequency: "daily"
  
  # 安全评估
  security_assessment:
    enabled: true
    rules:
      - name: "public_s3_bucket"
        severity: "critical"
        query: "SELECT * FROM aws_s3_buckets WHERE public_access_block = false"
      
      - name: "unencrypted_ebs"
        severity: "high"
        query: "SELECT * FROM aws_ebs_volumes WHERE encrypted = false"
      
      - name: "open_ssh"
        severity: "high"
        query: "SELECT * FROM aws_security_groups WHERE ingress->0->>FromPort = 22"
  
  # 合规监控
  compliance_monitoring:
    enabled: true
    frameworks:
      - "CIS AWS Foundations"
      - "PCI DSS"
      - "HIPAA"
      - "SOC2"
  
  # 自动修复
  auto_remediation:
    enabled: true
    rules:
      - name: "encrypt_unencrypted_ebs"
        action: "encrypt_volume"
        severity: "high"
      
      - name: "close_public_s3"
        action: "block_public_access"
        severity: "critical"
```

| CSPM功能 | 说明 | 重要性 |
|----------|------|--------|
| 资源发现 | 自动发现资源 | 高 |
| 安全评估 | 安全检查 | 高 |
| 合规监控 | 合规状态 | 高 |
| 自动修复 | 自动修复 | 高 |

### 云安全故障排查手册

| 故障现象 | 可能原因 | 排查步骤 | 解决方案 |
|----------|----------|----------|----------|
| 数据泄露 | 权限配置 | 检查IAM策略 | 修正权限 |
| DDoS攻击 | 流量异常 | 检查流量 | 启用防护 |
| WAF误拦截 | 规则配置 | 检查规则 | 调整规则 |
| 密钥泄露 | 密钥管理 | 检查密钥 | 轮换密钥 |
| 合规违规 | 配置错误 | 检查配置 | 修正配置 |
| 安全告警 | 异常检测 | 检查日志 | 调查处理 |

### 云安全监控与告警

```yaml
# 云安全监控配置
security_monitoring:
  # 安全指标
  metrics:
    - name: "security_events"
      description: "安全事件数量"
      threshold: 100
    
    - name: "failed_logins"
      description: "登录失败次数"
      threshold: 50
    
    - name: "policy_violations"
      description: "策略违规次数"
      threshold: 10
  
  # 告警规则
  alerts:
    - name: "high_security_events"
      condition: "security_events > 100"
      severity: "critical"
    
    - name: "many_failed_logins"
      condition: "failed_logins > 50"
      severity: "warning"
    
    - name: "policy_violations"
      condition: "policy_violations > 10"
      severity: "warning"
```

| 监控指标 | 说明 | 告警阈值 |
|----------|------|----------|
| security_events | 安全事件数 | >100 |
| failed_logins | 登录失败次数 | >50 |
| policy_violations | 策略违规次数 | >10 |

> 核心原则：**云原生安全，IAM最小权限，数据加密，审计合规，CSPM态势管理，安全监控到位**。

| 角色 | 职责 | 联系方式 |
|------|------|----------|
| 安全负责人 | 总体协调 | 手机+邮箱 |
| 运维负责人 | 系统恢复 | 手机+邮箱 |
| 开发负责人 | 代码修复 | 手机+邮箱 |
| 法务负责人 | 合规处理 | 手机+邮箱 |
| 公关负责人 | 对外沟通 | 手机+邮箱

> 一句话：**云安全 = 责任共担（云厂商保底层、用户保数据/应用/配置）+ 托管服务（KMS/WAF/DDoS/证书）+ 合规审计（CloudTrail/等保）；选型先看「合规区域与国密要求」，再用原生托管服务（便宜且生态联动好）。**
