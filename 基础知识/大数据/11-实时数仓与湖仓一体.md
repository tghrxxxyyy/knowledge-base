# 大数据 · 11 实时数仓与湖仓一体（实时链路 / Lakehouse 架构 / Iceberg-Paimon / 流批一体 / 落地决策）

> 业务要的不是"昨天的数据"，而是"现在的数据"。实时数仓让指标秒级更新；湖仓一体则把"湖的廉价灵活"与"仓的可靠分析"合二为一，是 2025 大数据架构的终点形态。本篇深入拆解实时链路（CDC+Flink）、湖仓一体（Iceberg/Paimon+对象存储）、流批一体落地与架构决策。

---

## 一、要解决的问题

| 痛点 | 说明 |
|------|------|
| 时效滞后 | 离线 T+1 跟不上业务 |
| 数据割裂 | 湖/仓两套存储，口径不一致 |
| 链路重复 | Lambda 双链路开发/运维成本高 |
| 实时与离线不一致 | 同指标实时/离线两套数 |
| 成本高 | 数据拷贝多份、计算重复 |

> 核心认知：**实时数仓 = 秒级指标（CDC→Kafka→Flink→OLAP/湖）**；**湖仓一体 = 对象存储 + 开放表格式获得仓能力，一份数据服务湖与仓**；**流批一体 = 同一 API/存储同时承流承批**。

---

## 二、离线 vs 实时数仓

| 维度 | 离线数仓 | 实时数仓 |
|------|---------|---------|
| 时效 | T+1（小时/天） | 秒/分钟 |
| 链路 | Sqoop/DataX → Hive Spark → OLAP | CDC → Kafka → Flink → OLAP/湖 |
| 引擎 | Spark/Hive | Flink + ClickHouse/Doris/StarRocks/Paimon |
| 场景 | 经营报表、宽表建模 | 实时大屏、风控、监控、推荐 |
| 一致性 | 强（批全量） | 需处理乱序/重复（见 [08](08-流处理计算：Flink.md)） |

---

## 三、实时数仓架构

### 3.1 链路全景

```mermaid
flowchart LR
    DB[(业务DB)] --> CDC[CDC Canal/Debezium]
    LOG[日志/埋点] --> KAFKA[(Kafka)]
    CDC --> KAFKA
    KAFKA --> FLINK[Flink: 清洗/维表join/聚合]
    FLINK --> OLAPI[OLAP: ClickHouse/Doris/StarRocks]
    FLINK --> LAKE[Paimon/Iceberg 实时入湖]
    OLAPI --> BI[实时大屏/API]
    LAKE --> BATCH[Spark 离线补齐/回刷]
```

### 3.2 关键机制

```
Kafka 削峰：生产突发 → 消息缓冲 → 消费平滑
维表 join：流式事实 join 维度（用户维度）→ 广播态/查询 HBase/Redis
精确一次：Flink Checkpoint + Sink 幂等/事务
大状态：明细聚合用 RocksDB 状态后端

实时分层（与离线对齐）：
  ODS：Kafka 原始变更流
  DWD：Flink 清洗/标准化/维表 join
  DWS：Flink 窗口聚合（宽表/主题指标）
  ADS：写 OLAP 或 Paimon 供查询
  DIM：维度放 HBase/Redis，Flink 实时 lookup
```

---

## 四、湖仓一体（Lakehouse）：终点形态

### 4.1 为什么需要

```
旧模式："数据湖（原始）" + "数据仓库（分析）"两套
  数据拷贝、口径不一致、成本高、管理难

湖仓一体：
  在廉价对象存储上用开放表格式获得仓的能力
  一份存储服务湖与仓 → 消除"湖仓双写"
```

### 4.2 核心组成

```mermaid
flowchart LR
    OBJ[(对象存储 S3/OSS)]
    OBJ --> FMT[开放表格式: Iceberg/Hudi/Delta/Paimon]
    FMT --> ENG1[Spark 批处理]
    FMT --> ENG2[Flink 流处理]
    FMT --> ENG3[Trino/StarRocks 交互查询]
    FMT --> ENG4[Python/ML 科学计算]
    CAT[Catalog: Polaris/Nessie/HMS] -.元数据.-> FMT
```

```
开放表格式提供：ACID/时间旅行/schema 演进（见 [05](05-列式存储与数据湖格式.md)）
Catalog 统一管理表元数据（开源 Polaris、Nessie；云 S3 Tables、Glue）
多引擎共用一份数据
```

### 4.3 关键能力

```
ACID 提交：并发写安全（快照隔离）
时间旅行：任意历史快照查询（回滚/审计）
schema 演进：加列/删列不重写数据
隐藏分区：按分区值自动管理（无需手动分区字段）

一份数据被批（Spark）、流（Flink）、交互（Trino）、ML（Python）共读
```

---

## 五、Paimon vs Iceberg（流式 vs 通用）

| 维度 | Iceberg | Apache Paimon |
|------|---------|---------------|
| 定位 | 通用开放表格式 | 流式湖仓（流写优先） |
| 写入 | 批写为主，CDC 流写增强中 | **Flink 流写原生**（LSM upsert） |
| 读取 | 快照读（批）+ 增量读 | **流读（changelog）+ 批读（快照）** |
| 更新 | upsert 需优化（delete files） | **原生主键 upsert** |
| 存储 | 对象存储 + manifest | 对象存储 + LSM（同表承流承批） |
| 生态 | 最广（Spark/Flink/Trino 全支持） | Flink 最强，Spark 支持增强 |
| 适用 | 通用湖仓、多引擎 | Flink 实时链路 + 湖仓 |

```
选型：
  通用互操作 + 多云 → Iceberg
  Flink 实时 upsert + 流读 → Paimon
  常见组合：Iceberg 底座 + Paimon 实时明细
```

### 5.1 Paimon 存储原理

```
LSM 架构（类似 HBase/RocksDB）：
  内存 buffer → 有序文件（sstable）→ 合并 compaction
  主键表：按主键 upsert（新旧合并）

流读：changelog 文件记录变更 → Flink 流式消费
批读：快照文件 → Spark/Trino 全量查询
同一张表：流读实时、批读离线，口径天然一致
```

---

## 六、流批一体（Unified）

### 6.1 为什么需要

```
Lambda 架构问题：
  批链路 + 流链路双开发
  两套代码两套口径 → 对不上

流批一体目标：
  同一份逻辑/代码处理有界（批）与无界（流）
  存储同时承接（Paimon/Iceberg）
```

### 6.2 实现方式

```
Flink：统一 API（批=有界流，同算子/SQL）
Spark Structured Streaming：同 DataFrame API
存储：Paimon/Iceberg 同时承流承批

收益：开发效率 ↑、口径一致 ↑、运维成本 ↓
```

### 6.3 Flink + Iceberg 实战

```sql
-- Flink 流写 Iceberg（upsert by 主键）
INSERT INTO iceberg.shop.orders
SELECT order_id, user_id, amount, ts FROM mysql_orders_cdc;

-- 同一张表 Spark 批读做 T+1 回刷/补齐
INSERT OVERWRITE iceberg.shop.orders
SELECT /* 全量校准 */ ... FROM staging;
```

```
同一张 Iceberg 表：Flink 流写保证实时性，Spark 批读保证全量校准
```

---

## 七、维度表实时更新（CDC + 湖表）

```
痛点：维度（用户/商品）变化需实时反映到事实查询
方案：
  MySQL 维度表 → Flink CDC → Iceberg/Paimon 维表
  事实流用 FOR SYSTEM_TIME AS OF 时态 join 实时打宽
  维度表作为 Iceberg 快照被 Spark 批读 → 离线口径一致
```

---

## 八、典型实时场景链路

| 场景 | 链路 |
|------|------|
| 实时大屏/GMV | MySQL binlog → Canal → Kafka → Flink 聚合 → StarRocks/Doris → 大屏 |
| 实时风控 | 日志/交易 → Kafka → Flink CEP/规则 → 命中即拦截 + 告警 |
| 用户实时画像 | CDC → Flink → HBase（宽表更新）+ 标签计算 → 推荐系统查询 |
| 日志监控 | Filebeat → Kafka → Flink → ClickHouse/Druid → Grafana |

---

## 九、湖仓一体查询实战（Trino/Iceberg）

```sql
-- Trino 直接查 Iceberg 表（无需导入）
SELECT city, SUM(pay_amount) AS gmv
FROM iceberg.shop.orders
WHERE ts >= TIMESTAMP '2026-07-29 00:00:00'
  AND ts <  TIMESTAMP '2026-07-30 00:00:00'
GROUP BY city;

-- 隐藏分区 + 时间旅行
SELECT * FROM iceberg.shop.orders
FOR TIMESTAMP AS OF TIMESTAMP '2026-07-28 12:00:00';
```

```
Trino 通过 Iceberg 连接器读 manifest 做文件级裁剪（min/max）
只对相关 Parquet 下推谓词 → 扫描极少文件
```

---

## 十、落地架构决策

### 10.1 分阶段演进

```
阶段 1（起步）：离线数仓（Spark + Hive + OLAP）
阶段 2（实时化）：实时数仓（Kafka + Flink + OLAP）补充关键指标
阶段 3（湖仓一体）：对象存储 + Iceberg/Paimon + Catalog 统一
阶段 4（流批一体）：Flink/Paimon 同表承流承批，消 Lambda
```

### 10.2 选型要点

| 决策点 | 选择 |
|--------|------|
| 存储 | 对象存储（S3/OSS）+ 表格式 |
| 表格式 | Iceberg（通用）/ Paimon（流式） |
| 流引擎 | Flink（实时标准） |
| 批引擎 | Spark（批标准） |
| 交互查询 | Trino / StarRocks 外表直查 |
| Catalog | Polaris/Nessie（开源）/ 云 Glue/S3 Tables |
| OLAP 明细 | ClickHouse（日志）/ StarRocks（BI） |

---

## 十一、生产 Checklist

- [ ] 实时与离线用同一套分层与口径（OneData），避免"两数"。
- [ ] 实时链路进 Kafka 削峰，Flink 保证 Exactly-Once。
- [ ] 新架构优先湖仓一体（对象存储 + Iceberg/Paimon）+ 流批一体。
- [ ] Catalog 统一管理，多引擎共用一份数据。
- [ ] 维表用 HBase/Redis 提速，大维表广播。
- [ ] 存储用对象存储 + Iceberg/Paimon，统一 Catalog。
- [ ] 定时 compaction + 过期快照清理，防小文件。
- [ ] 时间旅行用于回滚/审计，权限经 Ranger/Polaris。
- [ ] 监控：端到端延迟、CDC lag、checkpoint、compaction 滞后、OLAP 写入 QPS。

---

## 实时OLAP引擎深度对比

### 主流引擎特性

| 维度 | ClickHouse | Apache Doris | StarRocks | Apache Druid |
|------|-----------|-------------|-----------|-------------|
| 架构 | MPP列式 | MPP列式 | MPP列式 | Segment+索引 |
| 写入 | 批写/批量导入 | Stream Load/CDC | Stream Load/CDC | 实时摄入 |
| 查询延迟 | 亚秒 | 亚秒 | 亚秒 | 亚秒 |
| 并发 | 中(100~500) | 高(1000+) | 高(1000+) | 高(1000+) |
| 标准SQL | 部分 | MySQL兼容 | MySQL兼容 | Druid SQL |
| 物化视图 | ✅ | ✅ | ✅ | ✅ |
| 存算分离 | 有限 | ✅ | ✅ | ✅ |
| 生态 | ClickHouse原生 | MySQL生态 | MySQL生态 | 自有生态 |

```
选型建议：
  日志分析/时序 → ClickHouse（列压缩极优）
  高并发BI → StarRocks/Doris（MPP+物化视图）
  实时大屏 → Druid（预聚合+倒排索引）
  湖仓一体 → StarRocks/Doris（外表直查Iceberg）
```

### 流批统一模式

```
统一模式实现：
  Flink批=有界流（同一API/SQL）
  Spark Structured Streaming（同DataFrame API）
  存储：Paimon/Iceberg同时承流承批

口径一致保障：
  同一SQL逻辑 → 引擎自动适配批/流
  存储层：同一张Iceberg表，Flink流写+Spark批读
  消除Lambda双链路：一套代码两种模式
```

### CDC实时数仓架构

```mermaid
flowchart LR
    DB[(MySQL/PG)] --> CDC[CDC: Canal/Debezium]
    CDC --> KAFKA[(Kafka)]
    KAFKA --> FLINK[Flink 清洗/维表join]
    FLINK --> ODS_DWD[ODS/DWD 实时明细]
    FLINK --> DWS[DWS 实时聚合]
    DWS --> OLAP[OLAP: Doris/StarRocks]
    OLAP --> BI[实时大屏/API]
    ODS_DWD --> LAKE[Iceberg/Paimon]
    LAKE --> BATCH[Spark T+1回刷]
```

```
CDC关键点：
  Canal：MySQL binlog解析，JSON格式输出
  Debezium：多源CDC（MySQL/PG/MongoDB）
  Flink CDC：直接消费binlog，Exactly-Once
  数据格式：debezium-json / canal-json

维表join策略：
  广播维表（小维表）：全量加载到TaskManager内存
  查询维表（大维表）：lookup HBase/Redis
  时态表join：FOR SYSTEM_TIME AS OF（Iceberg）
```

### 物化视图刷新策略

| 策略 | 说明 | 适用 | 延迟 |
|------|------|------|------|
| 全量刷新 | 每次全量重算 | 小数据量 | 高 |
| 增量刷新 | 只计算变化部分 | CDC场景 | 低 |
| 实时刷新 | 流式聚合写入 | 实时数仓 | 秒级 |
| 手动刷新 | 按需触发 | 报表场景 | 按需 |

```
实现方式：
  Doris/StarRocks：物化视图自动刷新（Sync/Async）
  ClickHouse：物化视图（触发器式写入）
  Iceberg：增量读+Spark写物化视图
  Paimon：流式聚合表（天然实时物化视图）
```

### 实时大屏架构

```mermaid
flowchart TB
    subgraph 数据层
    DB[(业务DB)] --> CDC[CDC]
    CDC --> KAFKA[(Kafka)]
    end
    subgraph 计算层
    KAFKA --> FLINK[Flink实时聚合]
    FLINK --> REDIS[(Redis实时指标)]
    FLINK --> OLAP[OLAP: StarRocks]
    end
    subgraph 展示层
    REDIS --> WS[WebSocket推送]
    OLAP --> BI[BI看板]
    WS --> SCREEN[大屏展示]
    end
```

```
关键指标：
  端到端延迟：<5秒（CDC→大屏展示）
  查询QPS：大屏需支持100+并发
  数据一致性：实时/离线指标一致

技术选型：
  实时计算：Flink窗口聚合
  实时存储：Redis（热指标）+ StarRocks（明细查询）
  展示：WebSocket推送 + ECharts/Grafana
  刷新：实时推送 + 定时轮询混合
```

### 数据湖治理

```
湖仓治理核心：
  表格式治理：Iceberg快照管理、compaction、过期清理
  元数据治理：Catalog统一、Schema版本管理
  数据质量：湖表质量规则（非空、范围、一致性）
  安全治理：Ranger/Polaris权限、列级脱敏
  成本治理：生命周期管理、冷热分层

关键操作：
  Compaction：合并小文件（Iceberg rewrite manifests）
  快照清理：删除过期快照释放空间
  分区整理：删除过期分区
  统计信息更新：ANALYZE TABLE更新统计信息
```

### Schema-on-Read vs Schema-on-Write

| 模式 | 写入时 | 读取时 | 适用 |
|------|--------|--------|------|
| Schema-on-Write | 定义Schema，写入校验 | 直接读 | 数据仓库、强一致性 |
| Schema-on-Read | 原始写入，不校验 | 读时解析Schema | 数据湖、原始数据 |

```
湖仓融合：
  写入Schema-on-Write（Iceberg Schema校验）
  读取Schema-on-Read（灵活Schema演进）
  兼顾两者优势：写时质量+读时灵活

Iceberg实践：
  写入时绑定Schema ID
  读时可指定目标Schema（自动兼容）
  Schema Evolution：加列/改名不重写数据
```

### Lakehouse事务协议

```
ACID on Data Lake实现：
  快照隔离（Snapshot Isolation）：写不阻塞读
  原子提交（Atomic Commit）：多文件写入原子生效
  一致性读：读快照，不读部分写入

Iceberg事务机制：
  每次写入生成新快照（manifest文件）
  快照链：当前快照 → 父快照
  并发写：乐观锁+写入冲突检测
  读写分离：读旧快照，写新快照

Hudi事务机制：
  Timeline：所有操作按时间线记录
  Atomic Write：写入新文件+原子切换元数据
  读已提交（Read Committed）：只读已提交数据

Delta Lake事务：
  基于日志的MVCC（多版本并发控制）
  写入日志 → 提交 → 新版本
  读取时检查point-in-time快照
```

---

## 十二、实时 OLAP 引擎深度对比

### ClickHouse vs Doris vs StarRocks vs Druid vs Pinot

| 维度 | ClickHouse | Apache Doris | StarRocks | Apache Druid | LinkedIn Pinot |
|------|-----------|-------------|-----------|-------------|----------------|
| 架构 | MPP列式 | MPP列式 | MPP列式 | Segment+索引 | Segment+索引 |
| 写入 | 批写/批量导入 | Stream Load/CDC | Stream Load/CDC | 实时摄入 | 实时摄入 |
| 查询延迟 | 亚秒 | 亚秒 | 亚秒 | 亚秒 | 亚秒 |
| 并发 | 中(100~500) | 高(1000+) | 高(1000+) | 高(1000+) | 高(1000+) |
| SQL标准 | 部分 | MySQL兼容 | MySQL兼容 | Druid SQL | Pinot SQL |
| 物化视图 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 存算分离 | 有限 | ✅ | ✅ | ✅ | ✅ |
| 生态 | ClickHouse原生 | MySQL生态 | MySQL生态 | 自有生态 | 自有生态 |

### 选型决策树

```
选择OLAP引擎：
  日志分析/时序 → ClickHouse（列压缩极优）
  高并发BI → StarRocks/Doris（MPP+物化视图）
  实时大屏 → Druid（预聚合+倒排索引）
  湖仓一体 → StarRocks/Doris（外表直查Iceberg）
  事件分析 → Pinot（实时摄入+低延迟）
```

### 各引擎特点

```
ClickHouse优势：
  列压缩率极高（10:1+）
  向量化执行（SIMD）
  本地表+分布式表
  物化视图（触发器式）

Doris/StarRocks优势：
  MySQL协议兼容
  CBO优化器成熟
  存算分离（弹性）
  物化视图（自动刷新）

Druid优势：
  预聚合（倒排索引）
  实时摄入+查询
  高并发（1000+ QPS）
  时间序列优化

Pinot优势：
  实时摄入（秒级可查）
  低延迟（<100ms）
  高吞吐（100K+ QPS）
  与Kafka深度集成
```

## 流批统一模式详解

### 实现方式

| 方式 | 说明 | 引擎 |
|------|------|------|
| 批=有界流 | 同一API处理有界/无界数据 | Flink |
| 微批处理 | 流处理模拟批处理 | Spark Structured Streaming |
| 统一存储 | 同一存储承流承批 | Iceberg/Paimon |
| 统一SQL | 同一SQL处理批/流 | Flink SQL |

### 口径一致保障

```
口径一致方案：
  1. 同一SQL逻辑 → 引擎自动适配批/流
  2. 存储层：同一张Iceberg表，Flink流写+Spark批读
  3. 消除Lambda双链路：一套代码两种模式
  4. 元数据统一：Catalog管理表元数据
  5. 质量统一：同一套质量规则

验证方法：
  批处理结果 vs 流处理结果 → 对比验证
  离线指标 vs 实时指标 → 一致性检查
  跨系统对账 → 数据校验
```

## CDC 实时数仓架构

### 架构模式

```mermaid
flowchart LR
    DB[(MySQL/PG)] --> CDC[CDC: Canal/Debezium]
    CDC --> KAFKA[(Kafka)]
    KAFKA --> FLINK[Flink 清洗/维表join]
    FLINK --> ODS_DWD[ODS/DWD 实时明细]
    FLINK --> DWS[DWS 实时聚合]
    DWS --> OLAP[OLAP: Doris/StarRocks]
    OLAP --> BI[实时大屏/API]
    ODS_DWD --> LAKE[Iceberg/Paimon]
    LAKE --> BATCH[Spark T+1回刷]
```

### CDC 关键点

| 要点 | 说明 | 最佳实践 |
|------|------|----------|
| 数据格式 | debezium-json / canal-json | 统一格式 |
| 维表join | 广播/lookup/时态表 | 小维表广播，大维表lookup |
| Exactly-Once | Flink Checkpoint + Sink幂等 | 开启Checkpoint |
| 数据质量 | 实时质量规则 | 非空、范围、一致性检查 |
| Schema管理 | Schema Registry | 版本管理+兼容性检查 |

## 物化视图刷新策略

### 策略对比

| 策略 | 说明 | 适用 | 延迟 | 资源消耗 |
|------|------|------|------|----------|
| 全量刷新 | 每次全量重算 | 小数据量 | 高 | 高 |
| 增量刷新 | 只计算变化部分 | CDC场景 | 低 | 中 |
| 实时刷新 | 流式聚合写入 | 实时数仓 | 秒级 | 中 |
| 手动刷新 | 按需触发 | 报表场景 | 按需 | 低 |

### 实现方式

```sql
-- Doris/StarRocks 物化视图
CREATE MATERIALIZED VIEW mv_orders
AS
SELECT city, SUM(amount) as total_amount, COUNT(*) as order_count
FROM orders
GROUP BY city;

-- 刷新策略
REFRESH MATERIALIZED VIEW mv_orders;  -- 手动刷新
ALTER MATERIALIZED VIEW mv_orders SET ("refresh_times" = "ON_COMMIT");  -- 自动刷新

-- ClickHouse 物化视图（触发器式）
CREATE MATERIALIZED VIEW mv_orders
ENGINE = SummingMergeTree()
ORDER BY city
AS SELECT city, SUM(amount) as total_amount, COUNT(*) as order_count
FROM orders
GROUP BY city;
```

## 实时大屏架构

### 架构设计

```mermaid
flowchart TB
    subgraph 数据层
    DB[(业务DB)] --> CDC[CDC]
    CDC --> KAFKA[(Kafka)]
    end
    subgraph 计算层
    KAFKA --> FLINK[Flink实时聚合]
    FLINK --> REDIS[(Redis实时指标)]
    FLINK --> OLAP[OLAP: StarRocks]
    end
    subgraph 展示层
    REDIS --> WS[WebSocket推送]
    OLAP --> BI[BI看板]
    WS --> SCREEN[大屏展示]
    end
```

### 关键指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 端到端延迟 | <5秒 | CDC→大屏展示 |
| 查询QPS | 100+ | 大屏并发 |
| 数据一致性 | 100% | 实时/离线一致 |
| 可用性 | 99.9% | 高可用 |

### 技术选型

```
实时计算：Flink窗口聚合
实时存储：Redis（热指标）+ StarRocks（明细查询）
展示：WebSocket推送 + ECharts/Grafana
刷新：实时推送 + 定时轮询混合
监控：延迟、QPS、错误率
```

## Lakehouse 事务协议

### ACID 实现

```
ACID on Data Lake实现：
  快照隔离（Snapshot Isolation）：写不阻塞读
  原子提交（Atomic Commit）：多文件写入原子生效
  一致性读：读快照，不读部分写入
```

### 各格式事务机制

| 格式 | 事务机制 | 并发控制 | 读写分离 |
|------|----------|----------|----------|
| Iceberg | 快照链 | 乐观锁+冲突检测 | 读旧快照，写新快照 |
| Hudi | Timeline | 原子写入+元数据切换 | Read Committed |
| Delta | MVCC日志 | 多版本并发控制 | Point-in-time |
| Paimon | LSM | 快照隔离 | 读写分离 |

### 事务性能对比

```
事务性能指标：
  写入吞吐：Iceberg > Paimon > Delta > Hudi
  读取延迟：Paimon < Iceberg < Delta < Hudi
  并发能力：Iceberg > Delta > Hudi > Paimon
  存储开销：Paimon > Iceberg > Delta > Hudi

选型建议：
  通用湖仓 → Iceberg（平衡性能和功能）
  流式写入 → Paimon（Flink原生）
  Spark生态 → Delta（Databricks优化）
  传统Hive → Hudi（Uber发起）
```

## 实时数仓分层架构（ODS→DWD→DWS→ADS 实时层）

```
实时数仓分层（与离线对齐）：

  ODS（实时贴源层）：
    数据源：MySQL binlog（CDC）+ 埋点日志
    存储：Kafka Topic（原始事件流）
    特点：原样保留，不做转换

  DWD（实时明细层）：
    处理：Flink 清洗/标准化/维表 JOIN
    存储：Kafka Topic 或 Iceberg/Paimon 明细表
    特点：口径统一、维度打宽

  DWS（实时汇总层）：
    处理：Flink 窗口聚合（1min/5min/1h）
    存储：Doris/StarRocks 或 Paimon 聚合表
    特点：主题宽表、预聚合

  ADS（实时应用层）：
    处理：DWS 直接查询或简单二次计算
    存储：Redis（热指标）+ OLAP（明细查询）
    特点：面向应用、低延迟

分层价值：
  解耦：层间独立，变更不影响上层
  复用：DWD/DWS 被多个 ADS 共享
  口径一致：与离线分层对齐
```

```mermaid
flowchart LR
    DB[(MySQL)] --> CDC[CDC] --> K_ODS[(Kafka ODS)]
    LOG[日志] --> K_ODS
    K_ODS --> FL_DWD[Flink 清洗] --> K_DWD[(Kafka DWD)]
    K_DWD --> FL_DWS[Flink 聚合] --> OLAP[OLAP DWS]
    OLAP --> ADS[ADS 查询]
    K_DWD --> LAKE[Iceberg DWD]
    LAKE --> BATCH[Spark 离线补齐]
```

## 流批一体在 Flink 中的实现模式

```
Flink 流批一体实现：

  核心思想：批 = 有界流（同一 API/SQL）
  
  有界流（批模式）：
    Source: 读取文件/表（有界）
    Processing: 与流处理相同算子
    Sink: 写入文件/表
  
  无界流（流模式）：
    Source: Kafka/Socket（无界）
    Processing: 窗口/水印/状态
    Sink: 写入 OLAP/Kafka/文件

  同一 SQL 两种模式：
    SELECT city, SUM(amount) FROM orders
    WHERE ts >= '2026-01-01' AND ts < '2026-02-01'
    GROUP BY city;
    
    流模式：实时增量聚合（窗口）
    批模式：全量聚合（一次计算）
```

## 实时指标计算 vs 离线校准的一致性保障

```
一致性挑战：
  实时：秒级近似（可能丢事件/乱序）
  离线：T+1 准确（全量对账）

保障方案：
  1. 同一口径定义
     实时/离线用同一指标定义（OneData）
     DWS 层只算一次，ADS 只读不重算

  2. 最终一致
     实时结果 + 离线校准 = 最终准确
     实时允许近似（<0.1% 误差）
     离线全量对账后修正

  3. 对账机制
     实时指标 vs 离线指标 diff 检测
     差异超阈值 → 告警 + 人工修复

  4. 存储层保障
     Iceberg/Paimon 同一张表
     Flink 流写 + Spark 批读
     数据源唯一 → 口径天然一致
```

## 数据湖对 OLAP 引擎的透明化访问（Trino on Iceberg）

```
Trino 直接查 Iceberg 表（无需导入）：

  架构：
    Iceberg 表（S3/OSS）→ Trino 连接器 → SQL 查询
    无需数据拷贝，直接读 manifest 文件

  性能优化：
    文件级裁剪：manifest 中 min/max 索引
    谓词下推：WHERE 条件下推到 Parquet 层
    列裁剪：只读所需列
    分区裁剪：按分区值过滤

  配置：
    CREATE CATALOG iceberg_catalog
    USING iceberg
    WITH (s3.endpoint='http://minio:9000',
          s3.access_key='xxx',
          iceberg.file-format='PARQUET')

  查询：
    SELECT * FROM iceberg_catalog.db.orders
    WHERE ts >= TIMESTAMP '2026-07-01'
    AND city = '北京';
```

## 实时数仓成本优化（计算/存储/运维）

```
成本优化三维度：

  计算成本：
    Flink 按并行度计费 → 按实际吞吐动态扩缩
    避免全量重算 → 增量处理
    状态后端优化 → RocksDB 替代内存（减少 JVM 堆）

  存储成本：
    热数据：Doris/StarRocks（高性能 SSD）
    温数据：Iceberg/Paimon（对象存储）
    冷数据：Iceberg 低频存储 + TTL 清理

  运维成本：
    流批一体 → 一套代码两种模式
    存算分离 → 弹性扩缩
    监控自动化 → Prometheus + Grafana

量化示例：
  纯实时：Flink 集群常驻 + Doris 热存储 = 高成本
  实时+离线：Flink 增量 + Iceberg 冷存 = 成本降 50%+
```

## 实时数仓分层架构详解

### ODS→DWD→DWS→ADS 各层实时处理策略

```
ODS（实时贴源层）：
  数据源：MySQL binlog（CDC）+ 埋点日志
  存储：Kafka Topic（原始事件流）
  处理：Flink 原样消费，不做转换
  关键点：保证数据完整性，不做任何清洗

DWD（实时明细层）：
  处理：Flink 清洗/标准化/维表 JOIN
  存储：Kafka Topic 或 Iceberg/Paimon 明细表
  特点：口径统一、维度打宽
  关键点：维表 JOIN 用广播态或 lookup，避免全量扫描

DWS（实时汇总层）：
  处理：Flink 窗口聚合（1min/5min/1h）
  存储：Doris/StarRocks 或 Paimon 聚合表
  特点：主题宽表、预聚合
  关键点：窗口大小决定实时性与资源消耗

ADS（实时应用层）：
  处理：DWS 直接查询或简单二次计算
  存储：Redis（热指标）+ OLAP（明细查询）
  特点：面向应用、低延迟
  关键点：毫秒级响应，缓存策略关键
```

### 分层价值

| 价值 | 说明 |
|------|------|
| 解耦 | 层间独立，变更不影响上层 |
| 复用 | DWD/DWS 被多个 ADS 共享 |
| 口径一致 | 与离线分层对齐 |
| 治理 | 每层有明确的数据质量要求 |

## 流批一体在 Flink 中的实现模式

### Unbounded Table / Batch Table

```
Flink 流批一体实现：

  核心思想：批 = 有界流（同一 API/SQL）
  
  有界流（批模式）：
    Source: 读取文件/表（有界）
    Processing: 与流处理相同算子
    Sink: 写入文件/表
  
  无界流（流模式）：
    Source: Kafka/Socket（无界）
    Processing: 窗口/水印/状态
    Sink: 写入 OLAP/Kafka/文件

  同一 SQL 两种模式：
    SELECT city, SUM(amount) FROM orders
    WHERE ts >= '2026-01-01' AND ts < '2026-02-01'
    GROUP BY city;
    
    流模式：实时增量聚合（窗口）
    批模式：全量聚合（一次计算）
```

### 口径一致保障

```
口径一致方案：
  1. 同一 SQL 逻辑 → 引擎自动适配批/流
  2. 存储层：同一张 Iceberg 表，Flink 流写 + Spark 批读
  3. 消除 Lambda 双链路：一套代码两种模式
  4. 元数据统一：Catalog 管理表元数据
  5. 质量统一：同一套质量规则

验证方法：
  批处理结果 vs 流处理结果 → 对比验证
  离线指标 vs 实时指标 → 一致性检查
  跨系统对账 → 数据校验
```

## 实时指标计算 vs 离线校准的一致性保障机制

```
一致性挑战：
  实时：秒级近似（可能丢事件/乱序）
  离线：T+1 准确（全量对账）

保障方案：
  1. 同一口径定义
     实时/离线用同一指标定义（OneData）
     DWS 层只算一次，ADS 只读不重算

  2. 最终一致
     实时结果 + 离线校准 = 最终准确
     实时允许近似（<0.1% 误差）
     离线全量对账后修正

  3. 对账机制
     实时指标 vs 离线指标 diff 检测
     差异超阈值 → 告警 + 人工修复

  4. 存储层保障
     Iceberg/Paimon 同一张表
     Flink 流写 + Spark 批读
     数据源唯一 → 口径天然一致
```

## 数据湖对 OLAP 引擎的透明化访问

### Trino on Iceberg / Delta

```
Trino 直接查 Iceberg 表（无需导入）：

  架构：
    Iceberg 表（S3/OSS）→ Trino 连接器 → SQL 查询
    无需数据拷贝，直接读 manifest 文件

  性能优化：
    文件级裁剪：manifest 中 min/max 索引
    谓词下推：WHERE 条件下推到 Parquet 层
    列裁剪：只读所需列
    分区裁剪：按分区值过滤

  配置：
    CREATE CATALOG iceberg_catalog
    USING iceberg
    WITH (s3.endpoint='http://minio:9000',
          s3.access_key='xxx',
          iceberg.file-format='PARQUET')

  查询：
    SELECT * FROM iceberg_catalog.db.orders
    WHERE ts >= TIMESTAMP '2026-07-01'
    AND city = '北京';
```

## 实时数仓成本优化三策略

```
成本优化三维度：

  计算成本：
    Flink 按并行度计费 → 按实际吞吐动态扩缩
    避免全量重算 → 增量处理
    状态后端优化 → RocksDB 替代内存

  存储成本：
    热数据：Doris/StarRocks（高性能 SSD）
    温数据：Iceberg/Paimon（对象存储）
    冷数据：Iceberg 低频存储 + TTL 清理

  运维成本：
    流批一体 → 一套代码两种模式
    存算分离 → 弹性扩缩
    监控自动化 → Prometheus + Grafana

量化示例：
  纯实时：Flink 集群常驻 + Doris 热存储 = 高成本
  实时+离线：Flink 增量 + Iceberg 冷存 = 成本降 50%+
```

## 实时数仓监控指标设计

| 指标类别 | 关键指标 | 告警阈值 |
|---------|---------|---------|
| 数据延迟 | CDC lag、端到端延迟 | >5s |
| 数据质量 | 空值率、异常值比例 | >1% |
| 计算资源 | Flink 并行度、Checkpoint 耗时 | Checkpoint >10s |
| 存储 | Kafka 积压量、OLAP 写入 QPS | 积压 >10000 |
| 系统 | 节点负载、网络 IO | CPU >80% |

```text
监控体系：
  基础层：Kafka consumer lag、Flink metrics
  数据层：数据质量规则、口径一致性检查
  业务层：实时指标 vs 离线指标 diff
  告警层：钉钉/邮件/PagerDuty
```

> 一句话：**实时数仓 = CDC→Kafka→Flink→OLAP/湖 秒级链路；湖仓一体 = 对象存储 + Iceberg/Paimon（ACID/时间旅行）一份数据多引擎共用；流批一体 = Flink/Paimon 同表承流承批——落地四步：离线→实时化→湖仓一体→流批一体，核心守则：口径一致 + Catalog 统一 + compaction/快照清理**。

---

## 二十一、实时数仓分层架构（ODS → DWD → DWS → ADS 各层实时策略）

### 21.1 分层实时策略

| 层级 | 数据来源 | 实时策略 | 工具 | 延迟目标 |
|------|---------|---------|------|---------|
| ODS | CDC/Kafka | 原始数据透传 | Kafka Connect/Flink | < 1s |
| DWD | ODS | 实时清洗/关联 | Flink SQL | < 5s |
| DWS | DWD | 实时聚合 | Flink SQL | < 30s |
| ADS | DWS | 实时指标 | Flink/OLAP | < 1min |

### 21.2 分层架构示意

```mermaid
flowchart LR
    subgraph ODS[ODS 原始层]
        O1[CDC] --> O2[Kafka]
    end
    subgraph DWD[DWD 明细层]
        D1[Flink 清洗] --> D2[Iceberg/Paimon]
    end
    subgraph DWS[DWS 汇总层]
        S1[Flink 聚合] --> S2[OLAP 引擎]
    end
    subgraph ADS[ADS 应用层]
        A1[指标服务] --> A2[API/BI]
    end
    ODS --> DWD --> DWS --> ADS
```

## 二十二、流批一体 Flink 实现模式（Unbounded / Batch Table 统一）

### 22.1 Flink 流批一体语法

```java
// Flink SQL 统一流批
CREATE TABLE orders_stream (
    order_id BIGINT,
    amount DECIMAL(10,2),
    event_time TIMESTAMP(3),
    WATERMARK FOR event_time AS event_time - INTERVAL '5' SECOND
) WITH (
    'connector' = 'kafka',
    'topic' = 'orders',
    'properties.bootstrap.servers' = 'kafka:9092',
    'format' = 'json'
);

// 流查询
SELECT TUMBLE_START(event_time, INTERVAL '1' HOUR), SUM(amount)
FROM orders_stream
GROUP BY TUMBLE(event_time, INTERVAL '1' HOUR);

// 批查询（切换执行模式）
env.setRuntimeMode(RuntimeExecutionMode.BATCH);
env.execute("batch-job");
```

### 22.2 流批一体表格式

| 表格式 | 流写入 | 批读取 | 流读取 | 批写入 |
|--------|--------|--------|--------|--------|
| Iceberg | Flink Sink | 原生 | CDC | INSERT |
| Paimon | 原生 | 原生 | 原生 | INSERT |
| Delta Lake | Flink Sink | 原生 | CDC | MERGE |
| Hudi | Flink Sink | 原生 | 原生 | INSERT/MERGE |

## 二十三、实时指标计算 vs 离线校准一致性保障

### 23.1 一致性挑战

```text
实时 vs 离线一致性问题：
  实时：流处理近似计算（窗口/聚合）
  离线：批处理精确计算
  
  差异来源：
    1. 流处理延迟（乱序/迟到数据）
    2. 窗口边界（Tumbling/Sliding 窗口）
    3. 聚合精度（浮点/精度损失）
    4. 数据重复（至少一次语义）
```

### 23.2 一致性保障策略

| 策略 | 做法 | 适用场景 |
|------|------|---------|
| Lambda 架构 | 实时+离线双链路，离线校准 | 传统架构 |
| 离线校准 | 定时离线重算覆盖实时结果 | 精确要求高 |
| Exactly-Once | Flink Checkpoint + Sink 事务 | 端到端精确 |
| 幂等写入 | 下游幂等去重 | 语义精确 |

## 二十四、数据湖对 OLAP 引擎透明化访问（Trino on Iceberg）

### 24.1 Trino on Iceberg 配置

```sql
-- Trino 集成 Iceberg Catalog
CREATE CATALOG iceberg_catalog USING iceberg
WITH (
    'hive.metastore.uri' = 'thrift://metastore:9083',
    'iceberg.file-format' = 'PARQUET',
    'iceberg.compression-codec' = 'ZSTD'
);

-- 查询 Iceberg 表
SELECT * FROM iceberg_catalog.my_schema.my_table
WHERE date = '2024-01-01';

-- Time Travel
SELECT * FROM iceberg_catalog.my_schema.my_table
FOR SYSTEM_TIME AS OF TIMESTAMP '2024-01-01 00:00:00';
```

### 24.2 OLAP 引擎集成矩阵

| 数据湖格式 | Trino | Spark | Presto | Dremio | StarRocks |
|-----------|-------|-------|--------|--------|-----------|
| Iceberg | ✅ 原生 | ✅ 原生 | ✅ 原生 | ✅ 原生 | ✅ 原生 |
| Delta Lake | ✅ UniForm | ✅ 原生 | ⚠️ | ⚠️ | ⚠️ |
| Hudi | ✅ | ✅ 原生 | ✅ | ⚠️ | ⚠️ |

## 二十七、分层实时策略设计

### 27.1 实时数仓分层架构

```mermaid
graph TB
    A[ODS 原始层] --> B[DWD 明细层]
    B --> C[DWS 汇总层]
    C --> D[ADS 应用层]
    A -->|Flink CDC| B
    B -->|Flink SQL| C
    C -->|Flink SQL| D
    D -->|API| E[应用]
```

### 27.2 各层实时处理策略

| 层级 | 处理方式 | 引擎 | 延迟 |
|------|----------|------|------|
| ODS → DWD | CDC 实时同步 | Flink CDC | 秒级 |
| DWD → DWS | 实时聚合 | Flink SQL | 秒级 |
| DWS → ADS | 实时计算 | Flink SQL | 秒级 |
| ADS → 应用 | API 查询 | Doris/StarRocks | 毫秒级 |

---

## 二十八、流批一体 Flink 实现

### 28.1 流批一体架构

```text
Flink 流批一体核心：
  统一 API：DataSet/DataStream → Table API
  统一运行：同一个 Job 支持流/批模式
  统一语义： Exactly-once 语义

流模式：持续处理，事件驱动
批模式：有界数据集，一次性处理
```

### 28.2 流批一体 SQL

```sql
-- 流模式（持续查询）
CREATE TABLE orders_stream (
  order_id BIGINT,
  user_id BIGINT,
  amount DECIMAL(10,2),
  event_time TIMESTAMP(3),
  WATERMARK FOR event_time AS event_time - INTERVAL '5' SECOND
) WITH (
  'connector' = 'kafka',
  'topic' = 'orders',
  'properties.bootstrap.servers' = 'kafka:9092',
  'format' = 'json'
);

-- 批模式（有界查询）
CREATE TABLE orders_batch (
  order_id BIGINT,
  user_id BIGINT,
  amount DECIMAL(10,2),
  event_time TIMESTAMP(3)
) WITH (
  'connector' = 'filesystem',
  'path' = '/data/orders',
  'format' = 'parquet'
);

-- 同一个查询，流/批模式切换
SELECT 
  user_id,
  SUM(amount) as total_amount,
  COUNT(*) as order_count
FROM orders_stream  -- 或 orders_batch
GROUP BY user_id;
```

---

## 二十九、一致性保障机制

### 29.1 Flink 端到端一致性

```text
Flink 端到端一致性实现：
  Source：Kafka Consumer 支持 offset 提交
  Processor：Checkpoint + 2PC
  Sink：支持 2PC 的 Sink（如 Kafka、数据库）

Checkpoint 流程：
  1. JobManager 触发 Checkpoint
  2. Source 记录当前 offset
  3. Processor 保存状态快照
  4. Sink 预提交（2PC）
  5. 所有任务确认 → 提交
  6. 任一任务失败 → 回滚
```

### 29.2 一致性级别对比

| 级别 | 说明 | 性能 | 适用场景 |
|------|------|------|----------|
| AT-most-once | 最多一次 | 最高 | 允许丢失 |
| AT-least-once | 至少一次 | 中 | 允许重复 |
| Exactly-once | 精确一次 | 低 | 不能丢/重 |

---

## 三十、Trino on Iceberg 查询优化

### 30.1 Trino + Iceberg 配置

```sql
-- 创建 Iceberg Catalog
CREATE CATALOG iceberg_catalog USING iceberg
WITH (
    'hive.metastore.uri' = 'thrift://metastore:9083',
    'iceberg.file-format' = 'PARQUET',
    'iceberg.compression-codec' = 'ZSTD'
);

-- 查询优化
SET SESSION iceberg.split_target_size_bytes = 134217728;  -- 128MB
SET SESSION iceberg.max_split_size_bytes = 268435456;    -- 256MB
SET SESSION iceberg.compression_codec = 'ZSTD';

-- 查询 Iceberg 表
SELECT * FROM iceberg_catalog.my_schema.my_table
WHERE date = '2024-01-01';

-- Time Travel
SELECT * FROM iceberg_catalog.my_schema.my_table
FOR SYSTEM_TIME AS OF TIMESTAMP '2024-01-01 00:00:00';
```

### 30.2 查询优化技巧

| 优化点 | 说明 | 效果 |
|--------|------|------|
| 谓词下推 | 过滤条件下推到 Iceberg | 减少数据扫描 |
| 分区裁剪 | 跳过不相关分区 | 减少数据量 |
| 列式裁剪 | 只读取需要的列 | 减少 IO |
| 小文件合并 | 定期合并小文件 | 加速查询 |
| 统计信息 | 更新表统计信息 | 优化查询计划 |

---

## 三十一、实时数仓成本优化

### 31.1 成本优化策略

| 策略 | 做法 | 节省比例 |
|------|------|---------|
| 存储分层 | 热→温→冷自动迁移 | 30~50% |
| 计算弹性 | K8s HPA/Spot 实例 | 40~60% |
| 压缩优化 | ZSTD/Snappy 压缩 | 20~40% |
| 数据清理 | 清理过期数据 | 10~20% |
| 任务合并 | 合并相似任务 | 10~20% |

### 31.2 成本监控

| 指标 | 监控方式 | 告警阈值 |
|------|---------|---------|
| Kafka 存储量 | Kafka Exporter | > 100GB |
| Flink Checkpoint 大小 | Flink Metrics | > 10GB |
| OLAP 查询成本 | 资源使用量 | 超预算 20% |
| 存储增长率 | 增长趋势 | > 10%/月 |

---

## 三十二、实时数仓监控体系

### 32.1 监控指标体系

| 层级 | 指标 | 数据源 | 告警阈值 |
|------|------|--------|---------|
| 数据质量 | 数据完整性/准确性 | Flink SQL | 异常 |
| 数据时效 | 端到端延迟 | Flink Metrics | > 5min |
| 系统健康 | Kafka lag/Flink checkpoint | Prometheus | > 10000 |
| 业务指标 | 实时 vs 离线差异 | 自定义 | > 5% |

### 32.2 监控告警配置

```yaml
# Prometheus 告警规则
groups:
  - name: real-time-dw
    rules:
      - alert: KafkaConsumerLagHigh
        expr: kafka_consumer_lag > 10000
        for: 5m
        labels:
          severity: warning
      - alert: FlinkCheckpointSlow
        expr: flink_checkpoint_duration_seconds > 300
        for: 10m
        labels:
          severity: critical
      - alert: DataLatencyHigh
        expr: time() - max(event_timestamp) > 300
        for: 5m
        labels:
          severity: warning
```

---

## 三十三、实时数仓运维最佳实践

### 33.1 运维检查清单

```text
每日检查：
  ☐ Flink 任务状态（无失败）
  ☐ Kafka 消费延迟（< 10000）
  ☐ 数据延迟（< 5min）
  ☐ 资源使用率（< 80%）

每周检查：
  ☐ 小文件清理
  ☐ 慢查询分析
  ☐ 资源使用趋势
  ☐ 数据质量报告

每月检查：
  ☐ 存储容量规划
  ☐ 性能优化
  ☐ 架构评估
  ☐ 成本分析
```

---

## 二十五、实时数仓成本优化三策略

### 25.1 成本优化策略

| 策略 | 做法 | 节省比例 |
|------|------|---------|
| 存储分层 | 热→温→冷自动迁移 | 30~50% |
| 计算弹性 | K8s HPA/Spot 实例 | 40~60% |
| 压缩优化 | ZSTD/Snappy 压缩 | 20~40% |

### 25.2 成本监控

| 指标 | 监控方式 | 告警阈值 |
|------|---------|---------|
| Kafka 存储量 | Kafka Exporter | > 100GB |
| Flink Checkpoint 大小 | Flink Metrics | > 10GB |
| OLAP 查询成本 | 资源使用量 | 超预算 20% |

## 二十四、分层实时策略设计详解

### 24.1 分层架构各层职责

| 层级 | 数据延迟 | 数据格式 | 处理方式 | 存储 |
|------|---------|---------|---------|------|
| ODS | < 1s | JSON | Flink CDC | Kafka |
| DWD | < 5s | Avro | Flink SQL | Kafka |
| DWS | < 30s | Parquet | Flink SQL/窗口 | ClickHouse/Doris |
| ADS | < 1min | 自定义 | Flink SQL/批处理 | MySQL/Redis |

### 24.2 分层数据质量保障

```
质量保障机制：
  ODS层：数据完整性校验（schema验证）
  DWD层：空值处理/类型转换/去重
  DWS层：聚合计算正确性校验
  ADS层：业务规则校验

质量监控指标：
  数据完整性：缺失率 < 0.1%
  数据准确性：错误率 < 0.01%
  数据时效性：端到端延迟 < 1min
  数据一致性：离线vs实时差异 < 1%
```

## 二十五、流批一体Flink实现详解

### 25.1 Flink SQL流批一体核心

```sql
-- 流式查询（持续执行）
SELECT 
    window_start,
    window_end,
    user_id,
    COUNT(*) as event_count
FROM TABLE(
    TUMBLE(TABLE user_events, DESCRIPTOR(event_time), INTERVAL '1' HOUR)
)
GROUP BY window_start, window_end, user_id;

-- 批量查询（一次性执行）
SELECT 
    DATE_TRUNC('hour', event_time) as hour,
    user_id,
    COUNT(*) as event_count
FROM user_events
WHERE event_time BETWEEN '2024-01-01' AND '2024-01-02'
GROUP BY 1, 2;
```

### 25.2 流批一体关键配置

```
Flink SQL流批一体配置：
  table.exec.source.idle-timeout=30s      # 空闲分区超时
  table.exec.mini-batch.enabled=true       # Mini-batch优化
  table.exec.mini-batch.allow-latency=5s   # Mini-batch延迟
  table.exec.mini-batch.size=1000          # Mini-batch大小
  table.optimizer.agg-phase-strategy=TWO_PHASE  # 两阶段聚合

流批切换：
  execution.runtime-mode=batch/ streaming   # 运行时模式切换
  共用同一套SQL代码
```

## 二十六、一致性保障机制详解

### 26.1 Exactly-Once语义实现

```
端到端Exactly-Once保障：
  Source端：Flink Kafka Consumer（位点提交）
  Processing端：Checkpoint机制
  Sink端：两阶段提交（2PC）

两阶段提交流程：
  1. Pre-Commit：写入临时文件，不提交
  2. Checkpoint完成：提交临时文件
  3. Checkpoint失败：回滚临时文件

适用Sink：
  Kafka（事务性写入）
  文件系统（幂等写入）
  数据库（幂等写入）
```

### 26.2 状态一致性对比

| 一致性级别 | 实现方式 | 性能影响 | 适用场景 |
|-----------|---------|---------|---------|
| At-Most-Once | 不做Checkpoint | 无 | 允许丢数据 |
| At-Least-Once | Checkpoint但不保证Exactly-Once | 低 | 允许重复处理 |
| Exactly-Once | Checkpoint + 两阶段提交 | 中 | 金融/支付 |

## 二十七、Trino on Iceberg查询优化详解

### 27.1 查询优化策略

```
分区裁剪（Partition Pruning）：
  原理：根据查询条件裁剪不相关分区
  配置：iceberg.partition-spec=day
  效果：扫描数据量减少90%+

列裁剪（Column Pruning）：
  原理：只读取查询需要的列
  效果：IO减少50-80%

谓词下推（Predicate Pushdown）：
  原理：将过滤条件下推到存储层
  效果：减少数据传输量

数据跳过（Data Skipping）：
  原理：根据min/max统计信息跳过不相关数据块
  效果：查询延迟降低5-10倍
```

### 27.2 Trino Iceberg性能调优

| 调优项 | 配置 | 说明 | 优化效果 |
|--------|------|------|---------|
| 分区裁剪 | iceberg.partition-spec | 按时间/字段分区 | 扫描量↓90% |
| 列式读取 | iceberg.vectorized.enabled | 向量化执行 | CPU↓30% |
| 谓词下推 | 默认开启 | 过滤下推到存储层 | 传输量↓50% |
| 缓存 | iceberg.data.cache.enabled | 数据缓存 | 重复查询加速 |
| 并发 | iceberg.split.target-size | 分片大小 | 并行度↑ |

## 二十八、实时数仓成本优化详解

### 28.1 成本优化策略

| 策略 | 做法 | 节省比例 | 实施难度 |
|------|------|---------|---------|
| 存储分层 | 热→温→冷自动迁移 | 30~50% | 中 |
| 计算弹性 | K8s HPA/Spot实例 | 40~60% | 高 |
| 压缩优化 | ZSTD/Snappy压缩 | 20~40% | 低 |
| 数据生命周期 | 自动过期删除 | 20~30% | 低 |
| 资源隔离 | 队列/命名空间隔离 | 15~25% | 中 |

### 28.2 成本监控指标

```
成本监控指标：
  存储成本：$/GB/月
  计算成本：$/CU/小时
  网络成本：$/GB出流量
  API调用成本：$/1000次

优化建议：
  1. 定期清理过期数据（TTL策略）
  2. 启用压缩（ZSTD压缩率高）
  3. 使用Spot实例（节省60-80%）
  4. 监控资源使用率（避免过度配置）
  5. 使用对象存储替代HDFS（成本更低）
```

## 二十九、实时数仓监控体系详解

### 29.1 监控指标体系

| 层级 | 指标 | 数据源 | 告警阈值 | 处理方式 |
|------|------|--------|---------|---------|
| 数据质量 | 完整性/准确性 | Flink SQL | 异常 | 自动修复/告警 |
| 数据时效 | 端到端延迟 | Flink Metrics | > 5min | 扩容/调优 |
| 系统健康 | Kafka lag/checkpoint | Prometheus | > 10000 | 扩容/告警 |
| 业务指标 | 实时vs离线差异 | 自定义 | > 5% | 数据校准 |

### 29.2 监控告警配置

```yaml
# Prometheus告警规则
groups:
  - name: real-time-dw
    rules:
      - alert: KafkaConsumerLagHigh
        expr: kafka_consumer_lag > 10000
        for: 5m
        labels:
          severity: warning
      - alert: FlinkCheckpointSlow
        expr: flink_checkpoint_duration_seconds > 300
        for: 10m
        labels:
          severity: critical
      - alert: DataQualityAnomaly
        expr: data_quality_score < 0.95
        for: 15m
        labels:
          severity: critical
```

## 三十、实时数仓运维最佳实践

### 30.1 日常运维CheckList

```
每日检查：
  ☐ Kafka消费延迟
  ☐ Flink Checkpoint状态
  ☐ 数据质量报告
  ☐ 端到端延迟监控

每周检查：
  ☐ 资源使用趋势
  ☐ 数据质量趋势
  ☐ 成本分析报告
  ☐ 容量规划

每月检查：
  ☐ 存储容量规划
  ☐ 性能优化
  ☐ 架构评估
  ☐ 成本分析
```

### 30.2 故障处理流程

```
故障分级：
  P0（严重）：数据完全中断/核心业务受损
  P1（重要）：数据延迟>10min/质量严重下降
  P2（一般）：数据延迟>1min/质量轻微下降
  P3（低级）：监控告警/非核心功能异常

处理流程：
  1. 故障发现（监控告警/用户反馈）
  2. 故障定位（日志/监控/链路追踪）
  3. 故障处理（重启/扩容/回滚）
  4. 故障复盘（根因分析/改进措施）
```

## 二十六、实时数仓监控指标设计

### 26.1 监控指标体系

| 层级 | 指标 | 数据源 | 告警阈值 |
|------|------|--------|---------|
| 数据质量 | 数据完整性/准确性 | Flink SQL | 异常 |
| 数据时效 | 端到端延迟 | Flink Metrics | > 5min |
| 系统健康 | Kafka lag/Flink checkpoint | Prometheus | > 10000 |
| 业务指标 | 实时 vs 离线差异 | 自定义 | > 5% |

### 26.2 监控配置

```yaml
# Prometheus 告警规则
groups:
  - name: real-time-dw
    rules:
      - alert: KafkaConsumerLagHigh
        expr: kafka_consumer_lag > 10000
        for: 5m
        labels:
          severity: warning
      - alert: FlinkCheckpointSlow
        expr: flink_checkpoint_duration_seconds > 300
        for: 10m
        labels:
          severity: critical
```

---

## 二十七、实时数仓与湖仓一体最佳实践

### 27.1 架构选型

| 场景 | 推荐架构 | 说明 |
|------|----------|------|
| 实时大屏 | Flink + ClickHouse | 低延迟查询 |
| 风控系统 | Flink + Kafka + Redis | 实时决策 |
| 数据湖 | Flink + Iceberg/Paimon | 流批一体 |
| 机器学习 | Flink + Feature Store | 实时特征 |

### 27.2 落地步骤

```
实时数仓落地步骤：
  1. 梳理业务指标
     - 确定实时指标清单
     - 定义数据时效要求
     
  2. 设计技术架构
     - 选择 CDC 工具
     - 选择流处理引擎
     - 选择 OLAP 引擎
     
  3. 数据建模
     - ODS/DWD/DWS 分层
     - 维度建模
     
  4. 开发实施
     - CDC 数据采集
     - Flink 实时处理
     - OLAP 数据存储
     
  5. 运维监控
     - 数据质量监控
     - 性能监控
     - 告警配置
```

---

## 二十八、与其他板块的关系

- 离线数仓见「[09-数据仓库与OLAP引擎](09-数据仓库与OLAP引擎.md)」；
- 流处理见「[08-流处理计算：Flink](08-流处理计算：Flink.md)」；
- 数据湖格式见「[05-列式存储与数据湖格式](05-列式存储与数据湖格式.md)」；
- 消息队列见「[Kafka](../中间件/Kafka.md)」；
- OLAP 引擎见「[ClickHouse/Doris/StarRocks](./09-数据仓库与OLAP引擎.md)」。