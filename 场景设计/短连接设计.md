方法论：1.随机的6位，while循环判断数据库中是否已经存在了，不存在就插入

               2.基于数据库的自增主键，0-9a-zA-Z一共是62个，使用62进制产生6位

在2的方法下，如果需要支持分布式，需要保证全局唯一的序号，不够优美

可以使用Hash(long_url)%64作为sharding key 然后 short_url为 Hash(long_url)%64 + short_url

缺点是sharding的机器少于62台