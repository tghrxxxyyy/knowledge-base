# 数据格式与打包

> 工程效率维度。

## 一、核心概念

大规模训练用高效格式：JSONL(可读)、Parquet(列式)、WebDataset(流式分片)。打包(packing)把多条样本拼成固定长度序列减少 padding 浪费。

## 二、关键要点

- 流式读取避免全量载入内存。
- packing 提升吞吐但需 mask 隔断样本。

## 三、面试题

- 为何大规模训练偏好 Parquet/WebDataset 而非 JSONL？
