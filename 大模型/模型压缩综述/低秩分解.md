# 低秩分解 Low-Rank Factorization

> 权重矩阵近似低秩以压缩。

## 一、核心概念

把 `W ∈ ℝ^{m×n}` 近似为 `A·B`(`A∈ℝ^{m×r}, B∈ℝ^{r×n}`, `r≪m,n`)。与 LoRA 数学同构但目的不同：LoRA 用于微调，低秩分解用于压缩(预训练后分解权重)。

## 二、关键要点

- 适合低秩度高的权重。
- 常与量化组合。

## 三、面试题

- 低秩分解与 LoRA 的数学关系？
