# 推理模型评测

> 见「模型评测深入」。此处聚焦推理维度。

## 一、核心概念

推理模型评测看重：数学(AIME/MATH)、代码(LiveCodeBench)、科学推理(GPQA)、逻辑。需多次采样取最佳(pass@k)与均值，且评测集需防污染(用竞赛新题)。

## 二、关键要点

- 单次采样可能低估(思考偶发失误)。
- 成本/延迟也是评测维度。

## 三、面试题

- 为何推理模型评测常用 pass@k 而非 pass@1？
