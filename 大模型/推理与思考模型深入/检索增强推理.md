# 检索增强推理 RAG + Reasoning

> 推理模型与 RAG 的结合。

## 一、核心概念

复杂问答需「检索事实 + 推理整合」。推理模型可在思考中**多次检索**(如 Search-o1 边想边查)，把外部知识阶段性注入思考链，缓解长链中的事实遗忘与幻觉。

## 二、关键要点

- 检索时机(何时查)由模型自主决定更优。
- 检索结果需标注来源防注入。

## 三、面试题

- 推理模型为何可能比普通 RAG 更需要多次检索？
