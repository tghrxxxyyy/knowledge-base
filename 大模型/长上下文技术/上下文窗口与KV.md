# 上下文窗口与 KV Cache 扩展

> 见「推理优化/KV缓存」。长上下文下的显存压力。

## 一、核心概念

上下文越长，KV Cache 越大(线性于长度)。长上下文服务依赖 PagedAttention(分页)、KV 量化、以及稀疏/分块注意力降显存。

## 二、关键要点

- 长上下文推理的瓶颈常是 KV 显存而非算力。
- KV 量化(INT8/INT4)是常用降本手段。

## 三、面试题

- 为何长上下文服务显存瓶颈在 KV 而非权重？
