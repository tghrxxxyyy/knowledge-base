# 长文档 RAG

> 长上下文与 RAG 的关系。

## 一、核心概念

有了长上下文，是否还需要 RAG？结论：仍需。长上下文成本高、易 lost-in-middle、且超长仍需截断。RAG 用检索把最相关片段送入上下文，更省更准。两者可结合：RAG 召回 + 长上下文整合。

## 二、关键要点

- 长上下文不是 RAG 的替代品，而是补充。
- 检索质量决定上限。

## 三、面试题

- 有了 100 万 token 上下文，为什么还需要 RAG？
