# 长上下文继续训练

> 把短上下文模型扩展到长上下文。

## 一、核心概念

做法：在更长序列数据上继续预训练(或 SFT)，配合位置编码调整(NTK 缩放)、学习率调小、序列打包(packing)。数据需含长文档/长依赖，否则学不会长程。

## 二、关键要点

- 序列打包提升长样本利用率。
- 过长训练易不稳定，需 warmup。

## 三、面试题

- 长上下文继续训练为何要序列打包？
