# 自查询检索 Self-Query

> 结构化元数据过滤。

## 一、背景与挑战

用户问题常含过滤条件(时间/类型)，纯语义检索忽略。

## 二、核心原理

用 LLM 把自然语言拆成「语义查询 + 结构化过滤器」，先按元数据过滤再语义检索。

## 三、关键要点

- 元数据 schema 要清晰。
- 过滤器解析需校验。

## 四、面试题

- 为何 RAG 需结合元数据过滤？

## 五、小结

自查询提升检索精度。
