# 系统提示设计

> 上下文工程的核心组件。

## 一、核心概念

系统提示(system prompt)设定模型角色、目标、约束、输出格式、禁忌。设计要点：明确角色与职责、列出硬规则(不可违反)、定义输出 schema、给出降级策略、避免矛盾指令。

## 二、关键要点

- 硬规则用「必须/禁止」清晰表达。
- 输出 schema 与解析器对齐。
- 避免过长导致被模型忽略(中间遗忘)。

## 三、面试题

- 系统提示过长时为何可能被模型忽略？如何应对？
