# 上下文状态管理

> 多轮/多工具交互中的状态维护。

## 一、核心概念

状态包括：对话历史、用户上下文、工具调用结果、中间计划、记忆。需定义状态结构、更新时机、淘汰策略、跨请求持久化(会话存储)。Agent 中状态更复杂(轨迹)。

## 二、关键要点

- 状态与模型上下文分离(状态存外部，按需注入)。
- 敏感状态需权限隔离。

## 三、面试题

- 为何把状态存外部而非全放上下文？
