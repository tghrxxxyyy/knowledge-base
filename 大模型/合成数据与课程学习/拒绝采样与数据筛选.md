# 拒绝采样与数据筛选

> 对齐数据生产的关键技术。

## 一、核心概念

拒绝采样(rejection sampling)用模型生成多个候选，仅保留正确/高质量样本进训练集。常用于数学/代码 SFT 数据构造(如拒绝采样微调)，显著提升数据信噪比。

## 二、关键要点

- 生成多样 + 自动校验 + 保留优者。
- 是高质量推理数据的常用生产法。

## 三、面试题

- 拒绝采样如何提升 SFT 数据质量？
