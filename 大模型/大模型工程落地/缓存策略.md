# 缓存策略

> 降本提效的关键。

## 一、核心概念

- **精确缓存**：相同 query 直接返回。
- **语义缓存**：相似 query 命中(用嵌入相似度)。
- **前缀缓存**：系统提示等长前缀复用 KV Cache(见推理优化)。
- **结果缓存**：对确定性任务缓存最终答案。

## 二、关键要点

- 语义缓存需相似度阈值防误命中。
- 缓存易 stale，需 TTL/失效策略。

## 三、面试题

- 语义缓存相比精确缓存的取舍？
