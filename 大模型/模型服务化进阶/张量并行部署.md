# 张量并行部署

> 见「分布式训练/张量并行Megatron」与推理侧。

## 一、核心概念

单卡放不下大模型时，推理也可张量并行(多卡各持部分权重，层内 all-reduce)。vLLM/TRT-LLM 支持 `--tensor-parallel-size`。受限于卡间带宽(NVLink/PCIe)。

## 二、关键要点

- TP 受通信带宽制约，适合单机多卡。
- pipeline 并行跨节点但增延迟。

## 三、面试题

- 推理 TP 为何受限于卡间带宽？
