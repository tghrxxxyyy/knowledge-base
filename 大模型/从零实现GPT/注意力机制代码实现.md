# 注意力机制代码实现

> 对应 rasbt/LLMs-from-scratch 第3章「Understanding Attention」。手写单头与因果掩码注意力。

## 一、核心概念

书中用「简化自注意力」逐步引入：从单个 `x` 的线性投影，到批量矩阵化，再到因果掩码。关键是**因果掩码**保证自回归（位置 `i` 看不到 `>i`）。

## 二、代码实现（因果缩放点积注意力）

```python
import torch, torch.nn as nn
import torch.nn.functional as F

class CausalSelfAttention(nn.Module):
    def __init__(self, d_model, n_heads, ctx_len):
        super().__init__()
        self.d_model = d_model; self.n_heads = n_heads
        self.head_dim = d_model // n_heads
        self.qkv = nn.Linear(d_model, 3 * d_model)
        self.proj = nn.Linear(d_model, d_model)
        self.register_buffer("mask",
            torch.triu(torch.ones(ctx_len, ctx_len), diagonal=1).bool())

    def forward(self, x):
        B, T, C = x.shape
        q, k, v = self.qkv(x).split(self.d_model, dim=-1)
        q = q.view(B, T, self.n_heads, self.head_dim).transpose(1, 2)
        k = k.view(B, T, self.n_heads, self.head_dim).transpose(1, 2)
        v = v.view(B, T, self.n_heads, self.head_dim).transpose(1, 2)
        scores = (q @ k.transpose(-2, -1)) / (self.head_dim ** 0.5)
        scores = scores.masked_fill(self.mask[:T, :T], float("-inf"))
        w = F.softmax(scores, dim=-1)
        out = w @ v
        out = out.transpose(1, 2).contiguous().view(B, T, C)
        return self.proj(out)
```

## 三、关键要点

| 项 | 说明 |
|----|------|
| split qkv | 一次线性出三份 |
| 掩码 | 因果 |
| head_dim | d_model/n_heads |
| proj | 输出变换 |

## 四、常见误区

- 掩码用 0 而非 `-inf` 导致 softmax 仍分配概率。
- 未 `register_buffer` 掩码，device 不一致报错。

## 五、与开源书的对应

- rasbt/LLMs-from-scratch Ch.3: https://github.com/rasbt/LLMs-from-scratch

## 七、面试题

- 为何掩码要用 `-inf` 而非 0？
- 单头注意力与多头在代码上的核心差别？
