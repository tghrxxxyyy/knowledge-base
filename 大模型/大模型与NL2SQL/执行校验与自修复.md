# 执行校验与自修复

> 提升 SQL 可运行率。

## 一、背景与挑战

生成 SQL 常有语法/语义错误(字段不存在、类型不符)。

## 二、核心原理

把 SQL 在影子库执行，捕获报错，把错误信息回灌模型重试(ReAct 式)，直到可运行；再对结果做合理性检查。

## 三、关键要点

- 报错信息要结构化回灌。
- 需防误写危险语句(限 SELECT)。

## 四、常见误区

- 不校验直接返回，用户看到报错。
- 允许写操作致数据风险。

## 五、面试题

- 为何 NL2SQL 必须执行校验而非仅靠语法检查？

## 六、小结

执行校验+自修复大幅提升可用率。
