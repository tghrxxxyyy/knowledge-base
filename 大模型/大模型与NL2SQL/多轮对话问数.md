# 多轮对话问数

> 对话式数据分析。

## 一、背景与挑战

用户追问(「再按地区拆分」「去掉测试数据」)需基于上文改写 SQL。

## 二、核心原理

维护对话状态与上一 SQL，把追问转为对上一 SQL 的增量修改；或用「上一结果+追问」让模型重生成。

## 三、关键要点

- 保持上下文(上一 SQL/字段)。
- 追问可能改维度/过滤。

## 四、面试题

- 多轮问数如何避免每次从头生成 SQL？

## 五、小结

多轮状态管理是问数产品体验关键。
