# NL2SQL 权限与安全

> 见「安全与对齐深入」。

## 一、背景与挑战

用户可能问越权数据，或模型生成危险语句。

## 二、核心原理

行级权限在 SQL 注入过滤条件；禁止写操作；审计所有生成 SQL；敏感字段脱敏。

## 三、关键要点

- 生成 SQL 默认只读。
- 权限在 DB 层强制而非仅靠 prompt。

## 四、面试题

- 为何 NL2SQL 权限必须在数据库层强制？

## 五、小结

安全是 NL2SQL 上线红线。
