# 多模态模型演进

> 见「多模态深入」系列。

## 一、核心概念

从 CLIP 对比对齐(2021)，到 BLIP/ALIGN 图文预训练，再到 LLaVA 类「视觉编码器+投影+LLM」指令模型(2023)，到统一生成(Emu/Chameleon)。视频/音频模态逐步融入。

## 二、关键要点

- 早期：双塔对齐；现在：统一到 LLM 词空间。
- 视频是 2024+ 重点。

## 三、面试题

- 多模态模型从 CLIP 到 LLaVA 的核心架构变化？
