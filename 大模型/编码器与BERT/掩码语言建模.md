# 掩码语言建模 MLM

> BERT 预训练核心目标(见 BERT 架构解析)。

## 一、核心概念

MLM 通过预测被遮盖 token 迫使模型**双向**利用上下文，区别于 GPT 的单向。训练后，任意位置的表示都融合了左右文，适合抽取式理解。

## 二、关键要点

- 掩盖比例约 15%，过高/过低都伤训练。
- 微调时不再有 `[MASK]`，存在「预训练-微调不匹配」，BERT 用混合策略缓解。

## 三、面试题

- MLM 为何能学到双向表示？
