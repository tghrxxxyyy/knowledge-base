# BERT 微调范式

> 下游任务适配。

## 一、核心概念

BERT 微调在预训练头上加任务头：`[CLS]` 向量接分类头(单句/句对分类)、token 级头(NER/问答)。学习率小(2e-5~5e-5)、epoch 少(2~4)。

## 二、关键要点

| 任务 | 头 |
|------|-----|
| 分类 | `[CLS]`→MLP |
| NER | token→CRF |
| QA | 起止位置 |

## 三、面试题

- BERT 做 QA 如何预测答案区间？
