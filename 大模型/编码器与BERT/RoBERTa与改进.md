# RoBERTa 与改进

> Liu et al.(2019) 对 BERT 的训练增强。

## 一、核心概念

RoBERTa 证明「BERT 训练不足」：更大数据、更长训练、去掉 NSP、动态掩码、更大 batch，显著提升。启示：预训练范式比架构细节更重要。

## 二、关键要点

- 动态掩码：每 epoch 重新随机遮盖。
- 大数据 + 长训练是主要增益来源。

## 三、与开源书的对应

- Liu et al., *RoBERTa*, 2019.

## 七、面试题

- RoBERTa 相比 BERT 的主要改进是什么？
