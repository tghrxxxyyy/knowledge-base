# 零样本提示 Zero-Shot

> 对应 Prompt-Engineering-Guide「Zero-Shot」。

## 一、核心概念

零样本：不给任何示例，仅用自然语言描述任务，依赖模型预训练已学到的能力。大模型(尤其指令微调后)零样本表现强。适用分类、翻译、摘要、抽取等。

```
将下面句子分类为正面/负面/中性：
"这部手机续航很差，但屏幕很惊艳。"
输出：
```

## 二、关键要点

- 指令明确 + 输出格式约束是关键。
- 复杂任务零样本易失败，需转少样本或 CoT。

## 三、面试题

- 零样本为何在指令微调模型上可行？
