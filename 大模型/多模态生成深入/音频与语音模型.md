# 音频与语音模型

> 多模态的听觉维度。

## 一、核心概念

语音识别(Whisper)、语音合成(TTS)、语音对话(GPT-4o 类端到端)。表示：梅尔谱/离散 token。端到端语音模型直接处理音频流，降低延迟、保留韵律。

## 二、关键要点

- 音频 token 化是统一建模前提。
- 低延迟靠流式与端到端。

## 三、面试题

- 端到端语音模型相比 ASR+TTS 级联的优势？
