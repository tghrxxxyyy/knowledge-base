# 指令格式统一

> 对应 ChatML、OpenAI Chat Completions 格式与 HuggingFace chat template 实践。格式统一是复用与评测的前提。

## 一、背景与挑战

不同模型、不同阶段（SFT、推理、评测）如果各自使用不同的指令格式，会导致：同一份数据训出来的模型无法跨服务复用；推理时格式与训练不一致导致效果骤降；评测结果不可比。挑战是既要统一，又要保留特殊能力（工具调用、多模态、结构化输出）的表达空间。

## 二、核心原理

主流做法是把对话抽象成**角色 + 内容**的结构化列表，再由一个模板渲染成模型实际看到的字符串：

$$\text{prompt} = \mathcal{T}\big([(r_1,c_1),\dots,(r_n,c_n)]\big)$$

其中角色 $r\in\{\text{system},\text{user},\text{assistant},\text{tool}\}$，$\mathcal{T}$ 是模型特定的 chat template。关键原则：

1. **训练与推理用同一个 $\mathcal{T}$**，否则格式不匹配会严重掉点。
2. **特殊 token 集中管理**（如 `<|im_start|>`），不手写拼接。
3. **system 与 user 分离**：system 承载稳定约束，user 承载每次任务。

## 三、形式化与数学基础

给定模板 $\mathcal{T}$，训练损失仍只在 assistant 段计算：

$$\mathcal{L}=-\!\!\sum_{t: r_t=\text{assistant}}\!\!\log P_\theta(y_t\mid \mathcal{T}(\cdot), y_{<t})$$

模板差异可用「格式一致性」指标刻画：训练与推理使用 $\mathcal{T}_a,\mathcal{T}_b$ 时的性能差

$$\Delta = \text{score}(\mathcal{T}_b) - \text{score}(\mathcal{T}_a)$$

经验上 $\mathcal{T}_b\neq\mathcal{T}_a$ 时 $\Delta$ 常为显著负值。

## 四、代码实现

统一用 tokenizer 自带的 chat template，避免手写：

```python
msgs = [
    {"role": "system", "content": "你是严谨的数据分析助手，只输出 JSON。"},
    {"role": "user", "content": "把这段评论分类：续航差但屏幕好"},
]
prompt = tok.apply_chat_template(msgs, tokenize=False,
                                 add_generation_prompt=True)
```

结构化输出（工具调用）用受控 schema：

```python
tools = [{"type": "function",
          "function": {"name": "classify",
                       "parameters": {"type": "object",
                                      "properties": {"label": {"type": "string"}}}}}]
```

## 五、与其他技术对比

| 方案 | 通用性 | 可控性 | 复杂度 |
|------|--------|--------|--------|
| 裸文本拼接 | 差 | 低 | 低 |
| ChatML 角色标记 | 好 | 中 | 低 |
| chat template 自动渲染 | 很好 | 高 | 低 |
| 结构化工具 schema | 中 | 很高 | 中 |

## 六、常见误区

- 训练用一种格式、推理用另一种：最常见的掉点原因。
- 自己拼 `<|im_start|>` 等特殊 token：应由 tokenizer 管理。
- 把所有约束塞进 user：稳定约束应放 system。
- 忽略 add_generation_prompt：少了结尾提示，模型可能不开始回答。

## 七、与开源书·权威来源对应

- HuggingFace Transformers「Chat Templates」文档；OpenAI Chat Completions API 文档；以官方最新文档为准。

## 八、面试题

- 为什么训练与推理必须用同一套 chat template？
- system 与 user 消息应如何分工？
- 结构化输出为何比「要求模型输出 JSON」更可靠？

## 九、演进与趋势

chat template 成为模型发布的标准附带物；结构化输出/受限解码内置到推理引擎；工具调用格式逐步统一；模板本身也开始被版本化管理。

## 十、小结

指令格式统一的核心是「角色化 + 模板集中管理 + 训练推理一致」。用 tokenizer 的 apply_chat_template，别手写特殊 token，能避免绝大多数格式类问题。
