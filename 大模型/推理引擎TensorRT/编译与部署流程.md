# 编译与部署流程

> TRT-LLM 工程实践。

## 一、核心概念

流程：下载模型 → 用 `convert_checkpoint` 转 TRT 格式 → `trtllm-build` 构建引擎(选定量化/并行) → 用 `trtllm-serve` 或 Triton 部署。引擎与硬件绑定，换 GPU 需重建。

## 二、关键要点

- 引擎不可跨 GPU 架构移植。
- Triton 提供生产级服务封装。

## 三、面试题

- 为何 TRT-LLM 引擎不能跨 GPU 架构复用？
