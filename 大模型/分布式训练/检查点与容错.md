# 检查点与容错

> 长训练的工程保障。

## 一、核心概念

长时间训练需周期性存 checkpoint(参数/优化器/step/RNG)，支持从断点续训。要点：异步写(不阻塞训练)、多副本、校验、元数据(step/epoch)。容错：spot 实例被回收能续训。

## 二、关键要点

- 优化器状态 checkpoint 很大，需异步 IO。
- 续训需恢复 RNG 状态保可复现。

## 三、面试题

- 为何 checkpoint 要包含优化器状态？
