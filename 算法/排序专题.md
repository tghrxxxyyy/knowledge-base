# 排序专题（Sorting）

## 〇、本体介绍

**排序**是算法面试的"开场白"：背模板、比复杂度、讲稳定性，三件事一次考察完。本章覆盖**手写模板**（快排/归并/堆排/插入/冒泡/选择）、**经典复杂度对比表**、**稳定性分析**与**工程选型**，配套高频问答题。

**面试高频**：手写快排、归并排序（含链表版）、快排与归并的差异、稳定性、TopK 三种解法（见「[堆与优先队列](堆与优先队列.md)」）。

---

## 一、复杂度总览（背这张表）

| 算法 | 平均 | 最坏 | 最好 | 空间 | 稳定 | 备注 |
|------|------|------|------|------|------|------|
| 冒泡排序 | O(n²) | O(n²) | O(n)（已有序+标志位） | O(1) | ✅ | 简单，教学用 |
| 选择排序 | O(n²) | O(n²) | O(n²) | O(1) | ❌ | 交换次数最少 |
| 插入排序 | O(n²) | O(n²) | O(n)（近乎有序） | O(1) | ✅ | **小数组/近乎有序最优** |
| 希尔排序 | O(n^1.3) | O(n²) | O(n) | O(1) | ❌ | 插入的改进，分步长 |
| 归并排序 | O(n log n) | O(n log n) | O(n log n) | O(n) | ✅ | 稳定、外部排序基础 |
| 快速排序 | O(n log n) | **O(n²)**（有序+固定 pivot） | O(n log n) | O(log n) | ❌ | **平均最快**，工程默认 |
| 堆排序 | O(n log n) | O(n log n) | O(n log n) | O(1) | ❌ | 原地但缓存不友好 |
| 计数排序 | O(n+k) | O(n+k) | O(n+k) | O(k) | ✅ | 值域小、非比较 |
| 桶排序 | O(n+k) | O(n²) | O(n+k) | O(n+k) | ✅ | 数据均匀分布 |
| 基数排序 | O(d(n+k)) | O(d(n+k)) | O(d(n+k)) | O(n+k) | ✅ | 按位排序，d 为位数 |

> 记忆口诀：**"快"均快于堆；稳定看"冒插归基数桶计"；归并最坏也 O(n log n)，快排最坏退化 O(n²)。**

---

## 二、手写模板（面试必背）

### 2.1 快速排序（最常考）

```java
// 三数取中 + 双指针分区，规避有序数组退化
void quickSort(int[] a, int l, int r) {
    if (l >= r) return;
    int p = partition(a, l, r);
    quickSort(a, l, p - 1);
    quickSort(a, p + 1, r);
}

int partition(int[] a, int l, int r) {
    int mid = l + (r - l) / 2;
    // 三数取中：把 a[l],a[mid],a[r] 的中位数换到 l，做 pivot
    if ((a[l] > a[mid]) != (a[l] > a[r])) { /* a[l] 即中位数 */ }
    else if ((a[mid] > a[l]) != (a[mid] > a[r])) swap(a, l, mid);
    else swap(a, l, r);
    int pivot = a[l];
    while (l < r) {
        while (l < r && a[r] >= pivot) r--;   // 注意 >= 防死循环
        a[l] = a[r];
        while (l < r && a[l] <= pivot) l++;
        a[r] = a[l];
    }
    a[l] = pivot;
    return l;
}
```

- **复杂度**：平均 O(n log n)；**每次 partition 只切掉 1 个**时退化 O(n²)（有序数组 + 固定首元素 pivot）。
- **稳定性**：交换会打乱相等元素顺序 → 不稳定。
- **工程优化**：三数取中、随机 pivot、小区间（< 16）改用插入排序、重复元素三路切分。

### 2.2 归并排序（链表排序靠它）

```java
void mergeSort(int[] a, int l, int r) {
    if (l >= r) return;
    int mid = l + (r - l) / 2;
    mergeSort(a, l, mid);
    mergeSort(a, mid + 1, r);
    merge(a, l, mid, r);
}

void merge(int[] a, int l, int mid, int r) {
    int[] tmp = new int[r - l + 1];      // O(n) 辅助空间
    int i = l, j = mid + 1, k = 0;
    while (i <= mid && j <= r) tmp[k++] = a[i] <= a[j] ? a[i++] : a[j++];  // <= 保稳定
    while (i <= mid) tmp[k++] = a[i++];
    while (j <= r) tmp[k++] = a[j++];
    System.arraycopy(tmp, 0, a, l, tmp.length);
}
```

- **稳定**：合并时左半优先（`<=`），相等元素保持原序。
- **任何情况都是 O(n log n)**：适合对最坏复杂度敏感的场景。
- **链表排序**：`Merge Sort` 无需辅助数组（链表天然支持 O(1) 合并），LeetCode 148。

### 2.3 堆排序（见「[堆与优先队列](堆与优先队列.md)」）

建大顶堆 → 堆顶与末位交换 → 下沉，O(n log n)，原地不稳定。面试考原理多过手写。

### 2.4 插入排序（小数组/近乎有序最优）

```java
void insertionSort(int[] a) {
    for (int i = 1; i < a.length; i++) {
        int cur = a[i], j = i - 1;
        while (j >= 0 && a[j] > cur) {   // 大于才右移，相等不动 → 稳定
            a[j + 1] = a[j];
            j--;
        }
        a[j + 1] = cur;
    }
}
```

- 几乎有序时接近 O(n)；常数小，是快排小区间递归的收尾选择。
- JDK `Arrays.sort` 对**对象数组**用的就是"归并/插入变体（TimSort）"。

### 2.5 冒泡 / 选择（手写热身）

```java
// 冒泡：相邻交换，稳定；加 swapped 标志，有序即停
void bubbleSort(int[] a) {
    for (int i = 0; i < a.length - 1; i++) {
        boolean swapped = false;
        for (int j = 0; j < a.length - 1 - i; j++) {
            if (a[j] > a[j + 1]) { swap(a, j, j + 1); swapped = true; }
        }
        if (!swapped) break;
    }
}

// 选择：每轮找最小值放到前部，不稳定（跨距离交换）
void selectionSort(int[] a) {
    for (int i = 0; i < a.length - 1; i++) {
        int minIdx = i;
        for (int j = i + 1; j < a.length; j++) if (a[j] < a[minIdx]) minIdx = j;
        swap(a, i, minIdx);
    }
}
```

---

## 三、稳定性分析（为什么重要）

- **稳定的排序**：冒泡、插入、归并、计数、桶、基数。
- **不稳定的排序**：选择、希尔、快排、堆排。
- **排序稳定性有什么用？** 多字段排序时需要：先按时间排，再按分数排 → 若第二步稳定，则分数相同者仍保持时间序。数据库 `ORDER BY a, b`、表格多列排序都依赖这一点。

> 记忆口诀：**"选希快堆不稳定，冒插归计桶基稳"**。

---

## 四、工程选型（生产怎么排）

| 场景 | 选择 | 原因 |
|------|------|------|
| 通用排序（JDK `Arrays.sort`） | 快排（基本类型）/ TimSort（对象） | 平均最快；对象要稳定所以用归并变体 |
| 近乎有序（日志按时间几乎有序） | 插入排序 | O(n)，常数小 |
| 内存受限 + 最坏情况敏感 | 堆排序 | 原地 O(1) 空间，无 O(n²) |
| 稳定性要求（多字段排序） | 归并排序 | 稳定 + 最坏 O(n log n) |
| 超大文件（无法全部进内存） | 外部排序 = 归并 | 分块排序 + 多路归并 |
| 值域小（成绩 0~100、年龄） | 计数排序 | O(n+k) 线性 |
| 浮点/均匀分布大数据 | 桶排序 | 哈希到桶内各自排 |

> ⚠️ 实战提醒：**快排在有序数据上退化**是经典事故（误把固定 pivot 用在大数据量有序流上）；语言库默认排序都是调过的，自写排序要补随机 pivot 或三数取中。

---

## 五、TopK 与排序的关系（高频交叉题）

- **排序后取前 k**：O(n log n)，不划算。
- **堆（小顶堆容量 k）**：O(n log k)，数据流友好，见「[堆与优先队列](堆与优先队列.md)」。
- **快排 partition**：平均 O(n)——partition 返回位置 > k 只排左半，只排右半，平均砍半。BFPRT 可把最坏也做到 O(n)（工程少用）。

```java
// 快排思想求第 K 大：partition 后根据下标只递归一侧
int findKthLargest(int[] a, int k) {
    int l = 0, r = a.length - 1, target = a.length - k;
    while (true) {
        int p = partition(a, l, r);       // 复用 2.1 的 partition（升序）
        if (p == target) return a[p];
        else if (p < target) l = p + 1;
        else r = p - 1;
    }
}
```

---

## 面试高频问题（20+ 条）

1. **手写快排，说说复杂度？** 平均 O(n log n)，有序+固定 pivot 退化 O(n²)；空间 O(log n)（递归栈）。
2. **快排为什么不稳定？** partition 交换跨距离移动相等元素，可能越过与其相等的元素。
3. **归并为什么稳定？** 合并时左半 `<=` 右半先取，相等元素保持原顺序。
4. **为什么工程上对象排序用归并不用快排？** 稳定性要求：快排不稳定，TimSort（归并变体）稳定且对近乎有序数据 O(n)。
5. **堆排序为什么慢于快排？** 下沉跳跃访问内存（缓存不友好），快排顺序扫描。
6. **排序稳定性有什么用？** 多字段排序时保留前序排序的次序。
7. **近乎有序数组用什么排序？** 插入排序 O(n)（如 TimSort 的实际应用场景）。
8. **O(n log n) 是比较排序的下界？** 是，决策树模型：n! 种排列 ≥ 2^h，h ≥ log(n!) ≈ n log n。
9. **计数排序的限制？** 值域 k 大则内存爆炸；只能排整数（或可映射整数）。
10. **基数排序 vs 计数排序？** 基数按位多次排序（LSD/MSD），位数 d 决定趟数；计数一次到位。
11. **链表排序用什么？** 归并：链表切半无需随机访问，合并 O(1) 指针操作，空间 O(log n)（递归）或 O(1)。
12. **第 K 大除了排序还有啥？** 堆 O(n log k)、快排 partition 平均 O(n)、BFPRT 最坏 O(n)。
13. **快排优化手段？** 随机/三数取中 pivot、小区间插入排序、重复元素三路切分（三分快排）。
14. **外部排序是什么？** 数据超内存：分段读入内存排好写回（归并段），最后多路归并。
15. **希尔排序的增量怎么选？** 希尔序列/1,3,7…；最坏仍 O(n²)，但实际快于简单插入。
16. **稳定排序的稳定指什么？** 相等元素的相对顺序不变。
17. **Arrays.sort 基本类型为什么用快排？** 基本类型无需稳定，快排平均更快且常数小。
18. **Collections.sort 用什么？** TimSort：稳定、自适应（检测已有有序段合并）。
19. **什么场景排序会退化？** 快排：有序/逆序 + 固定 pivot；桶排序：数据全进一个桶。
20. **能否 O(n) 排序？** 非比较排序（计数/桶/基数）可，但依赖数据特性（值域/分布）。
21. **归并排序的空间能优化吗？** 手写可减少临时数组复用（仍 O(n)）；链表归并 O(1) 空间。
22. **三路快排解决什么？** 大量重复元素：=pivot 单独归堆，两边递归，避免退化成 O(n²)。
23. **排序在框架里的应用？** MySQL `filesort`（内存排序/归并外部排序）、Kafka 页缓存排序索引、ES 倒排合并。

---

[← 返回算法索引](README.md)

---

## 六、排序逐题精讲（含 Go 实现 + 复杂度）

### 6.1 快速排序（三数取中 + 双指针分区，Go）

```go
func quickSort(a []int, l, r int) {
    if l >= r { return }
    p := partition(a, l, r)
    quickSort(a, l, p-1)
    quickSort(a, p+1, r)
}
func partition(a []int, l, r int) int {
    // 三数取中：把中位数换到 l 作 pivot，规避有序退化
    mid := l + (r-l)/2
    if a[l] > a[mid] { a[l], a[mid] = a[mid], a[l] }
    if a[mid] > a[r] { a[mid], a[r] = a[r], a[mid] }
    if a[l] > a[mid] { a[l], a[mid] = a[mid], a[l] }
    pivot := a[mid]; a[mid] = a[l]; a[l] = pivot
    for l < r {
        for l < r && a[r] >= pivot { r-- }
        a[l] = a[r]
        for l < r && a[l] <= pivot { l++ }
        a[r] = a[l]
    }
    a[l] = pivot
    return l
}
// 平均 O(n log n)，最坏 O(n²)（固定 pivot + 有序）；空间 O(log n)（递归栈）。
```

### 6.2 归并排序（Go）

```go
func mergeSort(a []int, l, r int) {
    if l >= r { return }
    mid := l + (r-l)/2
    mergeSort(a, l, mid); mergeSort(a, mid+1, r)
    tmp := make([]int, r-l+1); i, j, k := l, mid+1, 0
    for i <= mid && j <= r {
        if a[i] <= a[j] { tmp[k] = a[i]; i++ } else { tmp[k] = a[j]; j++ }
        k++
    }
    for ; i <= mid; i++ { tmp[k] = a[i]; k++ }
    for ; j <= r; j++ { tmp[k] = a[j]; k++ }
    copy(a[l:], tmp)
}
// 任意情况 O(n log n)，空间 O(n)。稳定（合并用 <=）。链表版 O(1) 空间。
```

### 6.3 堆排序（Go）

```go
func heapSort(a []int) {
    n := len(a)
    for i := n/2 - 1; i >= 0; i-- { sink(a, i, n) }
    for i := n - 1; i > 0; i-- { a[0], a[i] = a[i], a[0]; sink(a, 0, i) }
}
func sink(a []int, i, n int) {
    for 2*i+1 < n {
        c := 2*i + 1
        if c+1 < n && a[c+1] > a[c] { c++ }
        if a[i] >= a[c] { break }
        a[i], a[c] = a[c], a[i]; i = c
    }
}
// 时间 O(n log n) 任何情况，空间 O(1) 原地；不稳定；缓存不友好（下沉跳跃）。
```

### 6.4 计数排序（非比较，Go）

```go
func countingSort(a []int, k int) []int { // 值域 [0,k]
    cnt := make([]int, k+1)
    for _, v := range a { cnt[v]++ }
    for i := 1; i <= k; i++ { cnt[i] += cnt[i-1] } // 前缀 → 位置
    res := make([]int, len(a))
    for i := len(a)-1; i >= 0; i-- { cnt[a[i]]--; res[cnt[a[i]]] = a[i] } // 逆序保证稳定
    return res
}
// 时间 O(n+k)，空间 O(n+k)。值域小、整数；稳定（逆序填）。
```

---

## 七、选型决策图（mermaid）

```mermaid
flowchart TD
    A[排序需求] --> B{需要稳定?}
    B -->|是| C[归并/计数/桶/基数/TimSort]
    B -->|否| D{最坏敏感/空间紧?}
    D -->|是| E[堆排序 O(1) 原地]
    D -->|否| F{通用最快?}
    F -->|是| G[快排/IntroSort]
    A --> H{值域小/分布好?}
    H -->|是| I[计数/桶排序 O(n)]
```

---

## 八、非比较排序深挖

| 算法 | 思想 | 复杂度 | 适用 |
|------|------|------|------|
| 计数排序 | 值域计数 + 前缀定位 | O(n+k) | 整数、值域小 |
| 桶排序 | 哈希到桶、桶内各自排 | O(n+k) 期望 | 均匀分布 |
| 基数排序 | 按位 LSD/MSD 多次稳定排 | O(d(n+k)) | d 位数整数/串 |

> 比较排序下界 O(n log n)（决策树：n! 排列 ≥ 2^h ⇒ h ≥ log(n!)）。非比较排序绕过"比较"假设，靠数据特性做到 O(n)，但**适用范围受限**（整数/可映射/分布好）。

---

## 九、外部排序（超内存文件）

数据远大于内存时：
1. **分块**：读入内存排序写回（归并段）。
2. **多路归并**：用堆（或 k 路归并）合并所有段，内存只留每段的当前元素。
3. 时间 O(n log n)（段数决定归并趟数）；MySQL `filesort`、ES 倒排合并都用此思想。

---

## 十、更多易错点（补充）

1. 快排 partition 用 `>=`/`<=` 防死循环（相等元素也要移动）。
2. 快排**不稳定**：跨距离交换越过相等元素；需稳定用归并。
3. 归并合并用 `<=`（左优先）保稳定；否则变不稳定。
4. 三数取中 / 随机 pivot 必加，否则有序数据退化 O(n²)。
5. 小区间（<16）可切插入排序，省递归常数。
6. 计数排序逆序填充才稳定；桶排序单桶溢出会变 O(n²)。
7. JDK `Arrays.sort` 基本类型用快排（双轴/IntroSort）、对象用 TimSort（稳定）。

---

## 十一、速记口诀与小结

> **口诀**：快排均快但最坏 O(n²)（加随机/三数取中）；归并稳、最坏也 O(n log n)；堆排原地不稳缓存差；计数桶基 O(n) 但值域受限；稳定看"冒插归计桶基"；选排看稳定性与最坏。

- 面试三连：**复杂度（平均/最坏/空间）→ 稳定性 → 工程选型（JDK/外部排序）**。
- 与 [堆与优先队列](堆与优先队列.md) 的堆排序同源；TopK 见 [堆与优先队列](堆与优先队列.md)。

---

## 十二、排序自测卡

| 自测题 | 你能秒答吗？ | 要点 |
|------|------|------|
| 手写快排 | ✅ 三数取中+双指针 | 见 6.1 |
| 手写归并 | ✅ 合并用 <= | 见 6.2 |
| 手写堆排 | ✅ 建堆+下沉 | 见 6.3 |
| 稳定排序有哪些 | ✅ 冒插归计桶基 | 口诀 |
| O(n) 排序条件 | ✅ 非比较+值域/分布 | 见八 |
| 外部排序 | ⚠️ 分块+多路归并 | 见九 |

> 自测标准：**快排 partition 不死的循环、归并稳定、非比较排序适用边界**三件事零失误。

---

## 十四、实战追问升级

| 追问 | 回答要点 |
|------|------|
| "快排为什么不稳定？" | 跨距离交换越过相等元素（见三） |
| "归并为什么稳定？" | 合并左优先（<=）保原序（见 6.2） |
| "IntroSort 是什么？" | 快排 + 堆排兜底 + 插入：最坏 O(n log n)，C++ std::sort |
| "三路快排？" | 大量重复元素：`=pivot` 单独堆，避免退化（见五） |
| "外部排序？" | 分块排 + 多路归并（见九） |
| "Arrays.sort 为何分类型？" | 基本类型快排（不稳但快）、对象 TimSort（稳定） |
| "比较排序下界？" | O(n log n)，决策树 log(n!)（见八） |

### 14.1 一道综合题：_排序链表（148）

```go
// 自底向上归并：先切长度 1,2,4... 的段两两合并，无需递归栈
func sortList(head *ListNode) *ListNode {
    if head == nil || head.Next == nil { return head }
    // 求长 + 用数组存节点，按值排序后重连（面试可简化为数组排序）
    var nodes []*ListNode
    for p := head; p != nil; p = p.Next { nodes = append(nodes, p) }
    sort.Slice(nodes, func(i, j int) bool { return nodes[i].Val < nodes[j].Val })
    for i := 0; i < len(nodes)-1; i++ { nodes[i].Next = nodes[i+1] }
    nodes[len(nodes)-1].Next = nil
    return nodes[0]
}
// 时间 O(n log n)，空间 O(n)（节点数组）。纯链表归并可 O(1) 空间（迭代切段）。
```

> 小结：**排序不是背模板，而是"复杂度/稳定性/工程选型"三件事**；链表排序用归并、海量用外部归并。

---

## 十三、相关主题反向链接

- 堆排/TopK：见 [堆与优先队列](堆与优先队列.md)
- 链表排序：见 [链表](链表.md)（148 归并 O(1) 空间）
- 多字段排序依赖稳定：见 [二叉树](二叉树.md)（TreeMap 排序）
